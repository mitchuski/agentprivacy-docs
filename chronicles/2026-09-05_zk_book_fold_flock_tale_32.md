# 2026-09-05 · The ZK Book fold, and the Flock names a face

*Family-B chronicle (agentprivacy-docs telling). Compresses to the master chronicle
`agentprivacy_master/docs/chronicles/2026-09-05_the-book-takes-a-name.md` — unsigned; the First Person's read comes
first. Nothing pushed, posted or deployed.*

## What moved

**The DTG ZK Book.** The recipe book for the ToIP DTG ZKP Task Force took its name from the spellbooks and its first
entry from Glenn Gore's ADR-001 (kept in the requester's own form, with a clause-by-clause crosswalk into recipe 010).
It gained a Primer chapter adapted from the Zero Knowledge Spellbook — the *Technical Bridge* layer only; Story,
Inscription and lattice never cross, and a test refuses their return — a Stacks chapter (lab Groth16 · ProveKit · SIROS
catalog · Flock), and a rite: a push into the task-force repository cannot obtain its commit footer until a proverb is
spoken, the spellbook's own relationship proverb protocol applied to publication. Staged for the upstream repository in a
worktree; nothing committed. Evidence repo: `dtgwg-zkp-mage` (`zkbook/`, `board/`).

**A frontier tale.** Flock — the binary-field SNARK that proves standard hashes at under 250× native cost so Ethereum can
aggregate hash-based signatures after the quantum transition — was first drafted as "Tale 31" in `grimoires/`, which
collided twice (Tale 31 is the Quest's opener; the v3.0 compilation is a parallel work). It is re-seated as **Zero Tale 32,
The Flock**, at `zero spells/32-tale-32.md`, naming vertex ⟨1,0,1,1,1,1⟩: Blade 61 (tale convention) · seated V47
(lattice lock). Counting agreed with walking: 23 + 38 = 61, 61/38 ≈ φ; complement pure Delegation, unnamed — a pair
opened. Sixteen named, forty-eight wait.

**The attachment method, written down.** `agentprivacy_master/docs/ZERO_FRONTIER_TALE_ATTACHMENT_METHOD_2026-09-05.md`
— Path A's six steps plus the registration half: `zero spells` → spellweb deploy copy → last page arithmetic → grimoire
model patch (v10.4.2 proposed: `models/privacymage_grimoire_v10_4_2_patch_zero-tale-32-flock.json`) → spellweb node and
edges (staged) → this chronicle and INDEX row → master chronicle → ZK Book primer (Bridge only) → reference sheet row.

## Open for the First Person

- Rule the blade convention for tales (two coexist since v10.4 reseated only the complement pair).
- Reconcile `zero spells/31-tale-31.md` with the spellweb deploy copy (they differ).
- Bind Tale 32, apply the patch, re-pin; or send the tale back to the forge.
- Relicensing line for the Primer (CC BY 4.0 over the adapted Bridges) before the ZK Book's commit C3.
