# 2026-08-17 · The Field Guide Convergence Merge

**Status:** session record (family-B root chronicle, non-pipeline). Master copy (source of
truth): agentprivacy_master `docs/chronicles/2026-08-17_field_guide_convergence.md`
(framework voice; unsigned at time of writing). Companion records: cityofmages
`tomes/tome-ix-the-horizon/10-the-predicate-in-the-street.md` (**PROPOSED act, unbound**),
spellweb `src/data/{nodes,edges}.ts` (the Field Guide weave, 9 nodes + 17 edges). In THIS
corpus the additions are: this chronicle + its INDEX row, the release manifest
(`2026-08-17_release_manifest_field_guide_wave.md`), and **the fold** —
`research/fieldguide/` (README + counter-spec + reflection map + question map + 2 NOTEs +
`runtimes/`, re-verified green in place: 124/124, vectors sha byte-identical to the field
run). Ruling 2026-08-17: `~/field_guide_privacymage` stays a git-less working directory;
the four canonical repos receive the updates. All uncommitted.

## What happened (2026-08-13 → 17, Field Guide × trust-graph lane)

1. **The field work arrived.** Two days' work from the field machine (24mitchuski) —
   `field_guide_privacymage` — copied into the home directory and merged into the corpus.
   Its subject: Max's ARWorld handoff (the Hitchhikers Field Guide, a walking AR client on
   OASIS whose "multiplayer" is Knowledge × Promise × Trust, not a shared room) answered by
   a counter-spec built as evidence — eight suites, 124 properties, all green as the field
   repo certified them (meet-overlay 54/54 · 22-code rejection register with deterministic
   vectors 8/8 · zero-shared-code Python consumer 15/15 byte-exact · a bridge importing the
   DTG lab's runtimes 07 + 01 10/10 · the reflection map itself checked 6/6, plus
   acceptance / questions / open-questions).

2. **The load-bearing findings, imported as certified.** The Gap survived the substrate
   change (agent-to-agent → phone-to-phone) with the deployment delta bounded at four
   asserted gates; the Field Guide trust graph is a pseudonym graph, not a personhood graph
   — corrected to the sharper reading that it is a **conformant pairwise deployment** of
   the DTG Credentials Core Spec, and the lab's formation runtime (which gates every edge
   on personhood) had never exercised the pairwise construction: the deployment found a
   hole in the lab. Rulings held by properties: a shared quest is not consent; audience is
   granted, not computed (two hops is nothing); a wiki fork is not an audience (publication
   is its own consent, `WORLD` deliberately not on the ladder); an alias is a migration; a
   coarsening rule that does not name its rounding mode is not a rule (JS half-up vs
   Python/C# half-even ⇒ same pin, 110 m apart); and a colleague's unrun acceptance
   checklist caught a public-tier peer-name leak the leak test missed.

3. **The wiki space federated home.** The 22-page space (built on the field farm as
   `privacy.fieldguide.localhost`, served to the team from the embassy Pis over the tailnet
   as `mitch.private.fish` / `fieldguide.pi4.private.fish`, pushed as lineage-preserving
   FedWiki forks) was seated as **fieldguide.localhost — the guide federation's 13th
   site**: 23 pages (+ a locally-written welcome), the assets workbench lane (63 files
   incl. the clone-and-run bundle), rostered on the guide hub, bound into the 14-host
   family, audited clean (0 broken · 0 leaks · 0 orphans). The two dated chronicle slugs
   are manifested circulating. Area named in the agentprivacy-wiki-sync registry before
   the build, per G1.

4. **The graph learned the street.** Spellweb gained the Field Guide weave:
   `gateway-field-guide`, `doc-field-guide-counter-spec`, six ruling concepts (meet rite ·
   audience-granted · pairwise peer reference · publication-consent · erosion-rate ·
   rounding rule), and the arc chronicle node (auto-sealed from atlas by the
   `type:chronicle` rule), wired to `tech-oasis`, `concept-dtg-credentials`,
   `concept-evidence-graph`, `skill-hitchhiker`. Atlas rebuilt: 811 nodes / 1,752 edges.

5. **Open / not done.** The suites have not been re-run on the home machine (the rounding
   finding is the argument for doing so); nothing sent to Max; the tome act is proposed,
   not bound; nothing committed or pushed anywhere — pushes are the First Person's act.
