# Plan: propagate the soil ruling across the canon

**Date:** 2026-07-16
**Serves:** the First-Person ruling that the seventh capital is land-based rentier capital (data as soil) and its four model evolutions.
**Source documents (read these first):**
- `research/pvm-v6-soil-and-the-programme-runtime-evolutions.md` — the ruling and the four evolutions (ledger L140).
- `papers/Programme/pipeline/reviews/SOIL_PROPAGATION_CHECKLIST_2026-07-15.md` — the file:line worklist by surface (ledger L141). **This plan sequences the checklist; the checklist holds the detail. Do not duplicate its line numbers here; open it beside this plan.**

**What this plan is not:** it is not authority to edit canon. The spec, the register (C55), the whitepaper, the Tomes, and spellweb are canon; their dispositions are the First Person's (GR-10). This plan marks, for each phase, what is register-gated (yours) versus what is a regenerable pipeline artifact (safe to execute once its gate opens). Nothing is pushed. P4 is never marked.

---

## The one thing that determines everything else

Almost every phase is downstream of a single unresolved question, so it is the gate, not merely step one:

> **Does WP-07's proven bound R(t) measure adversary reconstruction, or auditor certification?**

`rehydrations/academic/linear_cap_paper.md` currently states R(t)'s drift on decoder-class capacity and, in ER-6, **holds the true mutual information constant** ("only the certified value grows"). That is a certification-epistemics clock. The economics (evolution 3) need R(t) to be an **adversary-informational-capability erosion** of a fixed archive. Until this is decided, you cannot correctly re-pin the moving-ceiling chain, because you do not yet know whether the fix is a re-wording ("R(t) already is reconstruction, say so in informational terms") or a re-derivation ("the theorem proves certification, and the economics needs a different theorem"). This also **is** the sharpened M3 blocker on WP-14: the durability leg needs WP-07 to measure the right object, not merely to be citable.

**Consequence for sequencing:** Phase 1 is a decision gate. Phases 2 and 6-pipeline unblock only after it resolves. Phases 3, 4, 5 are register-gated regardless and can proceed in parallel with Phase 1 whenever the First Person chooses, since they do not depend on the R(t) metric answer.

---

## Safe-to-execute vs register-gated (the partition)

| Phase | Surface | Class | Who | Gate |
|---|---|---|---|---|
| 1 | WP-07 R(t) reconciliation | theory decision | A3 + First Person | none (this is the gate) |
| 2 | E2-C02 re-pin + evolution-3 chain sweep | pipeline extraction + rehydrations (regenerable) | A1/A0, then A3 verify | **blocked on Phase 1** |
| 3 | C55 character-change | canon: spec, register, whitepaper, Tome III, grimoire, spellweb nodes | First Person / register | none (parallel to 1) |
| 4 | Figure disposition (678×/31,000×) | canon: whitepaper, v4, spec §30, spellweb axiom | First Person / register (this is the L130 escalation) | none (parallel) |
| 5 | Supersede the immutable inscription | spellweb on-chain artifact | First Person | none (parallel) |
| 6 | Build the soil figure into framing | mixed: WEIS rehydrations (safe) + whitepaper/Tomes (canon) | A0 for pipeline part; First Person for canon part | pipeline part blocked on Phase 1 wording; canon part is yours |

**Safe for me to do next, without touching canon and without pre-empting the theory decision:** prepare the Phase-1 decision memo (below), and stage the Phase-2 and Phase-6-pipeline edits as *drafts against the regenerable artifacts* that apply the moment Phase 1 resolves. Everything in canon (Phases 3, 4, 5, and the canon half of 6) is yours.

---

## Phase 1 — Reconcile WP-07's R(t) (the gate)

**Goal:** a First-Person + A3 decision on what `linear_cap_paper.md`'s R(t) measures, and what the WP-14 durability leg is therefore allowed to claim.

**Steps:**
1. A3 reads ER-6 (L223/225), Remark 3.4, and the fading-wiretap precedent, and states plainly: is the theorem's drift (a) adversary reconstruction of a fixed archive under growing informational capability, (b) auditor certification of a fixed true value, or (c) separable into both?
2. Decision, three branches:
   - **(a) reconstruction** → the fix is wording only; carry an informational-capability restatement into ER-6 and the chain (Phase 2 is a re-wording).
   - **(b) certification** → the theorem does not underwrite the economics; either re-derive an adversary-reconstruction bound on the informational metric, or the WP-14 durability leg reverts to a stated open problem (honest fallback already drafted in WP-14 §6).
   - **(c) both** → separate the two clocks explicitly in WP-07 and cite only the reconstruction one in the economics.
3. Record the decision in the ledger and update the WP-14 M3 note to the resolved form.

**Done when:** the R(t) object is named, and WP-14's durability leg has a definite status (stands / conditional / open).

**My safe prep:** a decision memo laying out the three readings, the exact ER-6 passages, and what each branch does to WP-14 — for A3/First Person to rule on. No theory is decided by me.

---

## Phase 2 — Re-pin E2-C02, and sweep the evolution-3 chain (blocked on Phase 1)

**Goal:** the moving-ceiling definition and everything that inherits it speak the informational-capability clock the ruling names.

**Why E2-C02 first:** it is the single source. `extractions/E2-moving-ceiling.md` E2-C02 pins the drift to frontier-model/compute capability; the SoK, the essay, the policy brief, the P1 derivation, and the spec sections all inherit it. Fix the extraction, then sweep the inheritors.

**Steps (all regenerable pipeline artifacts; A1 rebuilds the extraction, A0 sweeps the rehydrations, A3 verifies):**
1. Restate E2-C02/C03/C04's C_S(t)+C_M(t) drift as driven by informational capability (linkage corpus + side priors) against fixed H(X). Supplement the two compute instances (Zcash flaw, Shor/quantum) with a linkage/de-anonymisation instance so the canonical examples match the clock.
2. Sweep the inheritors for residual compute wording (checklist A4-A7): `moving_ceiling_sok.md` Def 2, `the_moving_ceiling.md`, `enforceable_by_architecture.md`, `wp14_P1_derivation.md`. Reserve the word "compounds" for reputation throughout.
3. Note for the register (canon, not done here): spec §5.5 (244/246/254), §10.5:428, §25 instances, and `privacy_value_v6.md` C82 (62/66) carry the same compute framing and need the same re-pin under First-Person disposition.

**Done when:** no pipeline moving-ceiling artifact frames R(t) drift as compute; A3 confirms consistency with the Phase-1 R(t) decision.

---

## Phase 3 — Character-change the seventh capital to soil/rentier (register-gated)

**Goal:** C55 and its prose homes read as land-based rentier capital, with compounding scoped to reputational fertility only.

**Surfaces (all canon; First Person / register):**
- Register conjecture **C55** (`research/CONJECTURE_REGISTER_V6.md`; spec:892) — character to land/soil rentier; five predicates survive; compounding scoped to reputational fertility.
- **Whitepaper §§565-577 and 1529-1583** — the primary rewrite surface (fullest economic prose). Keep 1535/1583 (reputation compounds/appreciates) as the sole surviving compounding channel, labelled as such.
- **Tome III Act 9** (`cityofmages/tomes/tome-iii-selenes-witness/09-the-seventh-capital.md`) — reframe the compounding/accumulating spine to rent-by-position. Use the anchors: the Tome IX "the decoder moves, the data does not" epigram and the reputation-as-fertility register.
- **grimoire C55** (v1_9_3) — add the land/rentier qualifier (currently silent on type).
- **spellweb** `nodes.ts:1654/1756` — free-text soil/rentier specialisation (no schema change).
- Strengthen the existing bridge statement (spec:240 / v5_4:224, "the reconstruction ceiling is the mathematical guarantee that makes data dignity enforceable") to make guarantee-IS-ownability explicit: scoped disclosure is not a handover; ownership is durable only for the term R(t) permits.

**Done when:** the thesis reads as soil/rentier across the canon homes; the one legitimate compounding channel (reputation) is explicit; the inalienability bridge is stated.

---

## Phase 4 — Figure disposition (register-gated; this is the L130 escalation)

**Goal:** the retired multiples stop appearing as asserted facts.

**Surfaces (canon; First Person):** whitepaper:577 (31,000× "understood as…"), whitepaper:1581 ("scales superlinearly"), v4:219, spec §30 fence (1230-1239; tighten the prose-licensing), spellweb axiom node and legacy graph (`spellweb.jsx:28` "exponentially"). Retire 678×/31,000× as asserted facts; keep as fenced lineage; value = the appropriation share set by the consent-interface bargaining game.

**Note:** this is the ledger L130 CANON-LEVEL escalation the register already holds; Phase 4 is its execution surface.

**Done when:** no canon prose asserts the multiples as current measured facts.

---

## Phase 5 — Supersede the immutable inscription (register-gated)

**Goal:** the on-chain "7th capital compounds" record is corrected without pretending it can be edited.

**Surface:** `spellweb/public/data/inscriptions.json` line 1 (txid/hash-bound). It cannot be patched in place. Mint a **new** inscription that supersedes it with the rentier/soil reading, and record the supersession. First-Person act (it is a ledger-style canon record).

**Done when:** a superseding inscription exists and the old one is marked drift.

---

## Phase 6 — Build the soil figure into the framing

**Goal:** the land/soil analogy and the non-reconstruction=ownability bridge appear where the thesis is stated (currently a 0-hit gap; the rentier economics is present only as "market-position rent").

**Two parts:**
- **Pipeline (safe once Phase 1 sets the wording):** add the soil framing + the ownability bridge to the WEIS rehydration cluster framing sections (regenerable; A0).
- **Canon (yours):** the whitepaper thesis sections and Tome III, using the anchors already in canon — Tome IX "the decoder moves; the data does not," the reputation-as-fertility register, and the spellweb `market-remembers` field/season imagery reframed from compounding to rent.

**Done when:** the thesis carries the soil figure and states that the security guarantee is what makes the asset ownable and non-alienable.

---

## Standing rules for this plan

- Canon (`papers/`, `research/CONJECTURE_REGISTER_V6.md`, and by extension the whitepaper, Tomes, and spellweb canon artifacts) is read-only to roles. Phases 3, 4, 5, and the canon half of 6 are First-Person / register only.
- Pipeline extractions and rehydrations are regenerable; fix inputs and rebuild, never hand-patch generated output.
- GR-10: canon conflicts escalate, they do not get resolved by a role. This plan routes; the First Person rules.
- Nothing is committed or pushed. P4 is not marked.
- When a phase is executed, chronicle it (`chronicles/`), and the plan lifecycle is PLAN → EXECUTION → CHRONICLE.

## Immediate next action (when you return)

**Resolve Phase 1** — it gates the largest phase (2) and sharpens the WP-14 blocker.

**Status 2026-07-16 (ledger L143):** the safe prep is DONE and the gate awaits your ruling.
- Decision memo ready: `papers/Programme/pipeline/reviews/PHASE1_DECISION_MEMO_WP07_RT_2026-07-16.md`. Key result: **branch (a) is closed** — WP-07 denies the erosion reading explicitly in three review-hardened passages, and its model contains no adversary side-information variable, so no re-wording can produce it. The live choice is **(b) certification-only** (WP-14 durability leg withdraws to its own drafted open-problem fallback) versus **(c) two clocks** (a small re-derivation: model the adversary's corpus Z_adv(t) with Markov Z_adv–X–T; the linear cap survives conditioning and Fano-with-side-information puts the erosion in the DENOMINATOR, R_adv(t) = Σε_i / H(X | Z_adv(t)); routed to A3 as a proof obligation, unverified).
- Also found: WP-14 is internally split (S2 :285 informational vs Assumption A3 :566 frontier/numerator) — revision-loop fix in either branch.
- Phase-2 / Phase-6-pipeline drafts staged branch-conditionally: `papers/Programme/pipeline/reviews/PHASE2_PHASE6_STAGED_DRAFTS_2026-07-16.md` (note: E2-C03 mirrors register C82, so that row moves first under GR-1 even inside "safe" Phase 2).
