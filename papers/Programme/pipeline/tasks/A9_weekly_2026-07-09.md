---
role: A9
runtime: fable
branch: none (convention suspended)
gate_target: weekly suite green + three hardening items verified, or findings filed
due: 2026-07-09
---

# Task card · weekly consistency sweep + hardening verification · role A9

**Objective:** The cycle-6 sweep. Both TIER-A papers sit at awaiting-P4 and
the runtime may not touch them except to READ; this sweep is the post-churn
consistency record plus independent verification of the three hardening items
the last two cycles queued (L082(c), L091(d), L093(b)).

**The sweep:**
1. Run the four deterministic checks in `checks/` over the explicit file list:
   the two academic drafts (`rehydrations/academic/moving_ceiling_sok.md`,
   `linear_cap_paper.md`), the policy brief
   (`rehydrations/policy/enforceable_by_architecture.md`), the three public
   drafts (`rehydrations/public/the_moving_ceiling.md`, `the_uncarved_date.md`,
   `competence_without_history.md`), and `reviews/WP-04_prior_art.md`. Your
   run is the evidence of record.
2. HARDENING VERIFICATION (new this week; A0 applied the fixes on record,
   your verification is independent):
   a. L082(c) argv-less guard: invoke each of the four checks with NO
      arguments; each must exit 2 with the loud refusal, none may exit 0.
   b. L091(d) \bR\b fix: `check_versions.py reviews/WP-04_prior_art.md` must
      PASS (the :62 "or <1450" false positive is gone) AND a synthetic
      probe file containing an unconditioned "R < 1" paragraph must still
      FAIL (the GR-7 catch is not weakened). Use a scratch file outside the
      pipeline tree for the probe; delete it after.
   c. L093(b) prior_art staleness: confirm the A10 refresh (this cycle's card
      `PRIORART_A10_refresh_2026-07-09.md`, chronicle of this date) landed and
      the rows now agree with the A4-verified metadata; spot-check the
      Chevignard venue, Eigen Labs attribution, and Kagai row.
3. Cross-document consistency post-cycle-5 churn: WP-04 draft-v4 vs WP-07 —
   the notation divergence stays DELIBERATE per L069(c), confined to
   notation; every [E1-Cnn]/[E2-Cnn] marker in both trace maps resolves;
   register head C96 agrees manifest/register/checks (HEAD constant in
   check_register_refs.py).
4. NEW SURFACE: `extractions/E10-method-record.md` (built this cycle, A1)
   enters the sweep for the first time — frontmatter well-formed, claim
   numbering monotone, FEEDS block resolves, no TIER breach (extraction is
   internal; canon vocabulary IS permitted upstream), checks on the explicit
   path.
5. Known-tension registry: E1×check_versions, if present, is the known L024
   tension — record, do not fix.

**Out of scope:** fixing anything found (findings loop to owners); canon;
manifest; ledger (return proposed entries to A0); tracker regeneration
(GR-6, A0 rebuilds at close).

**Definition of done:** findings list (or explicit all-green statement) with
file/line anchors; the three hardening verdicts stated separately
(VERIFIED/FAILED each); chronicle per `chronicles/README.md`; proposed ledger
entry returned to A0.
