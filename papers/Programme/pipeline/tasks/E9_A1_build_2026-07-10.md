---
role: A1
runtime: fable
branch: none (convention suspended)
gate_target: E9 drafted (unblocks WP-22 zkproof-note; feeds WP-12 ongoing)
due: 2026-07-10
---

# Task card · E9 proving-substrate build · role A1 (Extractor)

**Objective:** build `extractions/E9-proving-substrate.md` — the
proving-substrate extraction (ZK proof systems, folding, the existence-leak
instance record, the blade-forge design). WP-22 (zkproof-note, TIER-S, due
next-workshop-call) consumes E9+E2; WP-12 (existence-leak hunt, ongoing)
feeds from it. The manifest note carries the standing discipline:
**L008-design-vs-deployment-stated-per-claim** — this corpus's known
integrity hazard is the ZK substrate being SPECIFIED, NOT IMPLEMENTED
(L008: forge_circuits empty; the real deployed crypto = SHA-256 + Ed25519 +
the kappa implementation). Every claim states design vs deployment
explicitly.

**Sources (manifest E9 list; resolve via SOURCES.md; respect integrity
flags):** spec§15, spec§27, C81-C84, folding-citations, shor-mage-record,
blade-forge-spec, aletheia-lethe-note, proof-packet-spec, kappa-impl,
schrottenloher-note.

**Discipline:**
- E1 format exactly; E9-Cnn at register head C96.
- Conjecture-status material (C81 existence-leak at ~70%, C82-C84 as the
  register holds them) carries register number + confidence VERBATIM from
  research/CONJECTURE_REGISTER_V6.md (GR-1). E2 already owns several of
  these at claim level (E2-C07/C08/C14/C15) — cross-reference ownership
  rather than duplicate (the E7 ownership-block pattern, L113): E9's angle
  is the SUBSTRATE (what proof systems, what folding schemes, what the
  packet formats specify), not the moving-ceiling claims.
- shor-mage-record: attribution discipline per the standing record — the
  `-main` directories are OTHERS' work vendored; the method layer is the
  programme's and unrun; state this per claim (the L008 class).
- schrottenloher-note: Chevignard-Fouque-Schrottenloher = CRYPTO 2025
  (the corrected venue, L070); the note's claims at their dated strength.
- Depth per source stated in the header (the L103 fence pattern); checks
  read BEFORE drafting (the L113 ordering lesson — this corpus is
  saturated with fenced-figure and static-ceiling hazard classes).
- Contradictions filed, not smoothed (GR-10); the E7 build found one
  CANON-LEVEL item — this corpus's canon-adjacent surfaces get the same
  scrutiny.

**Checks:** four `checks/` scripts on the new file with the explicit
path; all must PASS.

**Out of scope:** WP-22 drafting; canon; manifest (A0 flips E9 on
receipt); ledger (return proposed entry to A0); other extractions.

**Definition of done:** E9 drafted with claim count + per-source depth;
FEEDS naming WP-22/WP-12 per claim with E2 cross-references explicit;
checks recorded; chronicle per `chronicles/README.md`; proposed ledger
entry returned to A0.
