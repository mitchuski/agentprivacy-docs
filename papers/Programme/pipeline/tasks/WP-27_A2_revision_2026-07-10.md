---
wp: WP-27 (methods-paper-conjecture-governance)
role: A2
runtime: fable
tier: A
branch: none (convention suspended)
gate_target: draft-v3 (prose legs of the L107 loop; completes the revision; A0 re-check follows)
due: 2026-07-10
---

# Task card · WP-27 revision 1, prose legs · role A2 (Register Translator)

**Objective:** discharge the A2-routed findings of the A5 review 1 (memo on
`tasks/WP-27_A5_2026-07-10.md`; A0 rulings in L107) in
`rehydrations/academic/conjecture_governance_method.md`. A3's formal legs
have already landed (their addendum is in the handoff comment block; read
the A3 chronicle of this date) — consume A3's `[A2: place in section 7]`
block for MAJOR-3 and do not undo any A3 edit.

**Your legs:**
1. **MAJOR-1 (:310):** reword so "generation four" keeps E10-C24's
   denotation (the deterministic non-vacuous checks) and the 111/111 catch
   is attributed to the extraction sweep operating under the
   trace-or-delete discipline. Check your wording VERBATIM against
   E10-C24's claim text before writing; the reviewer's what-satisfies line
   is "any wording checked verbatim against E10-C24 suffices". This
   finding is the paper's own failure mode having fired — consider (your
   judgement) one clause in section 4.4 acknowledging that this
   misattribution was itself caught by the method's review station, which
   is true, on the record (L107), and exactly the paper's thesis; if you
   add it, cite nothing beyond this revision's record.
2. **MAJOR-3 (:356):** restate the transfer hypothesis using A3's block:
   the operationalised period, the invariant-named adoption criterion,
   criterion (i) as an observable with the counterfactual clause deleted,
   and the severally-vs-jointly meaning of a null on (i)-(iii). Remove
   A3's comment block once consumed.
3. **MAJOR-4 (:338 area, under the A0 ruling in L107):** one section-6
   paragraph stating CURRENT FACT: the conjecture register has a public
   surface (the one URL already cited); the ledger and chronicles are
   unpublished at draft time; their release or editorial availability is a
   decision reserved to the programme's principal and is recorded here as
   reserved, not promised. No new commitment minted.
4. **MINOR-3 (:122):** abstract's "two independent instances" gains the
   one-team qualifier section 6 declares decisive (e.g. "two independent
   instances within one programme") — N1 fence wording elsewhere
   untouched.
5. **MINOR-4 (:320):** strike "and a trace discipline enforced by
   deletion" from the S2 residue list, or narrow it to what the sweep row
   states; the memo's strike option is preferred (the trace discipline is
   already claimed at N1-assembly level; the S2 residue must not widen).
6. **MINOR-6 (:122):** abstract "prevent" -> detection wording ("make
   history rewriting detectable" class), consistent with :128 and the
   procedural-enforcement honesty (an I7 violation is NOT even detectable
   from the ledger alone — pick wording that does not overclaim; "forbid"
   by rule is acceptable, "prevent" is not).

**After the legs:** bump frontmatter to draft-v3; add your addendum to the
handoff comment block; verify the trace map's nothing-exceeds-STATUS line
is true again (MAJOR-1's fix restores it — check the line and update its
wording only if needed).

**Checks:** four `checks/` scripts on the file with the explicit path; all
must PASS.

**Out of scope:** A3's discharged legs (do not re-edit); the novelty fence
wording beyond the two abstract touches specified; canon; manifest; ledger
(return proposed entry to A0); extractions.

**Definition of done:** draft-v3; per-leg resolution list; check runs
recorded; chronicle per `chronicles/README.md` (before/after for the
MAJORs); proposed ledger entry returned to A0.
