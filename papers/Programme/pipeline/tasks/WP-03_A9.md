---
wp: WP-03 (unblocker; also standing suite sweep)
role: A9
tier: internal
extraction: none
branch: none (pipeline tree untracked in git; branch convention suspended until first-person commits)
gate_target: n/a (audit; findings unblock WP-03's A9-hygiene-pass blocker)
due: 2026-07
---

# Task card · WP-03 unblock · role A9 (Consistency Auditor)

**Objective:** Run the whitepaper hygiene pass that L003 records and WP-03 is blocked on, plus the standing suite-wide sweep, filing findings rather than fixing (canon is read-only).
**Inputs:** `GROUND_RULES.md`; ledger L003 (`reviews/critiques_ledger.md`); the whitepaper (resolve `whitepaper-PT`/whitepaper slugs via `SOURCES.md`; L003 names `swordsman_mage_whitepaper_v6_3.md`); `PAPERS_INDEX` if present in the papers tree; `checks/*.py`; `manifest.yaml` (read-only).
**Output:** findings as proposed ledger entries (exact format per `templates/ledger_entry_format.md`) plus a summary count; no canon edits, no artifact edits.
**Definition of done:**
- all four checks run suite-wide (every file under `rehydrations/` plus the whitepaper); per-check result reported; use `python` or `python3`, whichever the host resolves;
- whitepaper version references reconciled item by item: header vs document-metadata version, header date vs edition note, retired Research Paper v4.x citations, unconditioned static-ceiling passages (GR-7), canonical-figures fence (GR-3);
- each finding carries file, line, finding, proposed resolution;
- `checks/check_register_refs.py` HEAD constant verified equal to manifest `register_head` (C96);
- checks pass or findings filed, A0 notified with the batch for the human's weekly decision list.
**Out of scope:** editing the whitepaper or any canon file (GR-6/GR-10: file, never fix); editing check scripts without a ledger entry; touching the manifest.
**Handoff:** A0 receives the findings batch; WP-03's `blocker: A9-hygiene-pass` field clears only when A0 applies the manifest change after the human's L003 decision.
