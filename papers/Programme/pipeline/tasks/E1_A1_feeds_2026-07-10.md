---
role: A1
runtime: fable
branch: none (convention suspended)
gate_target: E1 filter-note clause + FEEDS reconciliation per the L111 execution note
due: 2026-07-10
---

# Task card · E1 WP-09 filter/FEEDS reconciliation · role A1 (Extractor)

**Objective:** the micro-card L111 queued. WP-09 (BGIN discussion paper)
drafted from E1 under three A0 rulings; E1's filter note and FEEDS lines
now reconcile to match, mirroring the L031 method. This is a hygiene touch:
claim STATEMENTS do not change.

**The legs (L111 execution note verbatim governs):**
1. Add a WP-09 clause to E1's TIER-S filter note: design-status claims
   (e1-c06/c36/c37/c38) are admissible at TIER-S in the DISCUSSION-PAPER
   genre as design descriptions with implementation status verbatim (the
   L111(a) ruling: the WP-02 operation-sequence fence is genre-specific to
   the policy brief, not tier-wide).
2. FEEDS: add WP-09 to c06, c36, c37, c38 (design-feed).
3. FEEDS: add WP-09 to c07, c08, c19, c22, c31, c32 annotated as
   open-question-feed (the L111(b) ruling: consumed as labelled open
   questions only, never as claims).
4. FEEDS: remove WP-09 from c17 and c18 (the L111(c) ruling; the L031
   interpretive-grounding precedent).
5. Note the reconciliation datedly in the filter note (the L031 pattern).

**Checks:** four `checks/` scripts on E1 with the explicit path; all
results as before the touch (the known L033 tension, if it appears in
versions, is recorded-not-fixed and pre-existing).

**Out of scope:** claim statements; any other extraction; canon; manifest;
ledger (return proposed entry to A0); rehydrations.

**Definition of done:** the five legs; check runs recorded; chronicle per
`chronicles/README.md` (short is fine — this is a micro-card); proposed
ledger entry returned to A0.
