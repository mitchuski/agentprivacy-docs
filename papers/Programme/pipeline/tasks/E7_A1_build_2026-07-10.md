---
role: A1
runtime: fable
branch: none (convention suspended)
gate_target: E7 drafted (unblocks WP-06 ieee7012-note, due 2026-09; feeds WP-20/WP-23)
due: 2026-07-10
---

# Task card · E7 identity-vrc build · role A1 (Extractor)

**Objective:** build `extractions/E7-identity-vrc.md` — the identity /
verifiable-relationship-credential extraction. WP-06 (ieee7012-note, due
2026-09) is the nearest undrafted deadline in the atlas and consumes E7;
WP-20 (ToIP DTG profile) and WP-23 (DIF trusted-agents item) feed from it
later. WP-06's chain does NOT start this cycle (max_standards_in_flight: 2
is at limit with WP-02 + WP-09); this build is the enabler so the chain can
start the moment a slot opens.

**Sources (manifest E7 list; resolve every slug via `SOURCES.md`; respect
integrity flags):** whitepaper-stack, spec§19, dtg-36-posts,
did-cid-convergence, myterms-package, ieee7012-plan, vrc-protocol-spec,
hearthold-build, star-holospace, tome-x. Note L049's registry ruling: the
VRC protocol document body is version authority v3.4, the filename stays
vrc_promise_protocol_v3_3.md — carry the version per that ruling.

**Format and discipline:** E1 format exactly (frontmatter, E7-Cnn claim
table, per-claim source trace per GR-9, FEEDS block, register head C96).
Claim statuses per the honest record: protocol-design claims are
Design-assumption unless the canon proves them; implementation claims carry
the L008 discipline (design vs deployment stated PER CLAIM — the myterms
extensions, hearthold, and star surfaces have different build statuses;
never present specified as built); conjecture-status material carries its
register number and confidence per the register (GR-1 — never invent or
promote). Where identity claims touch the separation/erasure cluster
already extracted in E1 (e.g. key custody, per-session destruction), FEEDS
cross-reference rather than duplicate (the E2-C11/C12-duplicate-E1 pattern:
say which extraction owns the claim).

**Coverage honesty:** state depth per source in the header (the E10
precedent, L103: the depth statements are the fence); defer honestly rather
than skim-and-overclaim. tome-x is canon-narrative — claims extract from
what it BINDS (decisions, definitions), not its prose colour, and canon
vocabulary maps to GR-4 terms in the claim text.

**Checks:** four `checks/` scripts on the new file with the explicit path;
all must PASS.

**Out of scope:** WP-06 drafting; canon; manifest (A0 flips E7 on receipt);
ledger (return proposed entry to A0); other extractions.

**Definition of done:** E7 drafted with claim count + coverage statement;
FEEDS block naming WP-06/WP-20/WP-23 per claim; checks recorded; chronicle
per `chronicles/README.md`; proposed ledger entry returned to A0.
