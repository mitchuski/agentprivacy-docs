---
role: A9
runtime: fable
branch: none (convention suspended)
gate_target: weekly suite green, or findings filed
due: 2026-07-08
---

# Task card · weekly consistency sweep · role A9 (Consistency Auditor)

**Objective:** The weekly suite run, with one first: the `rehydrations/academic/`
files (moving_ceiling_sok.md at draft-v2/P0, linear_cap_paper.md at
draft-v1/P0) enter the sweep for the first time.

**The sweep:**
1. Run the four deterministic checks in `checks/` (register_refs, tier_vocab,
   figures_fence, versions). A0 ran them green on 2026-07-08 open; re-run and
   record — your run is the evidence of record.
2. Cross-document consistency over the two academic drafts: theorem numbering
   internally consistent; every `[E1-cnn]`/`[E2-Cnn]` trace marker in each
   draft's trace map resolves to an existing claim in
   `extractions/`; the WP-04/WP-07 notation divergence (declared budgets vs
   class-relative effective capacities) is DELIBERATE per L069(c) — verify it
   is still confined to notation and has not become a claim conflict.
3. Version/status coherence: manifest.yaml status+gate per WP vs each draft's
   own frontmatter status line vs the tracker's board (rebuild NOT needed —
   read `tracker/` as-is, A0 rebuilds at close).
4. Known-tension registry: E1×check_versions FAIL, if present, is the known
   L024 tension — record, do not fix.

**Out of scope:** fixing anything found (findings loop to owners); canon;
manifest; ledger (report proposed entries to A0); tracker regeneration
(GR-6, A0's).

**Definition of done:** findings list (or explicit all-green statement) with
file/line anchors; chronicle per `chronicles/README.md`; proposed ledger
entry (if any finding is ledger-worthy) returned to A0.
