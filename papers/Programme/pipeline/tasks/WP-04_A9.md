---
wp: WP-04 (moving-ceiling-sok, TIER-A, HotPETs-or-FC-workshop)
role: A9
runtime: fable
tier: A
branch: none (convention suspended)
gate_target: P3 (four checks green + consistency pass) -> awaiting-P4
due: 2026-07-09
---

# Task card · WP-04 P3 consistency station · role A9 (Consistency Auditor)

**Objective:** The final runtime station for WP-04. The paper
`rehydrations/academic/moving_ceiling_sok.md` is at draft-v4, gate P1
(L092: review-1 loop closed, targeted re-check passed). Run the P3 pass; if
green, WP-04 flips to awaiting-P4. The WP-07 station (L081/L082) is the
precedent — including what it caught.

**The pass:**
1. Four deterministic checks, EXPLICIT FILE LIST (argv-less is vacuous,
   L082) — your run is the P3 evidence of record.
2. Internal consistency after the churn (revision 1 L090 + Kagai sweep L091
   + MINOR-4 pins L089 + Mosca-Piani retarget landed the same day):
   proposition/lemma/remark/conjecture numbering and cross-references
   resolve (note the NEW Remark 2 — check nothing else claims that number);
   contribution list vs body claims (the WP-07 catch class: any counting or
   blanket claim the revisions outdated — necessity counts, "each/every"
   claims, the two-unresolved→one narrowing consistent at every mention);
   Kagai named consistently (engaged in 5.1, positioned in 8, in the
   reference list, absent from any "unresolved" phrasing); no surviving
   pre-revision wording for the existence-leak law ("stated as a formal
   inequality" without the vacuity clause).
3. Trace map/apparatus vs body: [E2-Cnn] markers resolve against
   extractions/E2-moving-ceiling.md; the handoff-comment apparatus blocks
   (A3 formal pass, revision 1, L062(d) sweep) match what the body carries;
   the L069(c) notation-divergence flag still describes reality.
4. Frontmatter vs manifest coherence (manifest: draft-v4, gate P1; report
   mismatches, A0 amends).
5. Bib sanity: every citation key used resolves in pv_v6.bib (61 entries;
   key-presence grep, not a compile — the A4 compile of record stands).

**Out of scope:** fixing anything (findings block the flip, loop to A0);
canon; manifest; ledger (proposed entry in report); tracker regeneration.
Known and NOT findings: the prior_art.md check_versions :62 false positive
("r <1450", L091); the WP-04/WP-07 notation divergence (L069(c)); the one
unresolved preprint (deliberate, fenced).

**Definition of done:** P3 verdict (GREEN -> flip recommended, or findings
blocking); chronicle `chronicles/2026-07-09_a9-wp04-p3.md`; report to A0
with evidence table + proposed ledger entry (L0nn).
