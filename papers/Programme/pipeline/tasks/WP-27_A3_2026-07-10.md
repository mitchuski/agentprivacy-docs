---
wp: WP-27 (methods-paper-conjecture-governance)
role: A3
runtime: fable
tier: A
extraction: E10
branch: none (convention suspended)
gate_target: draft-v2 (the four formalisation stubs discharged; theorem discipline throughout)
due: 2026-07-10
---

# Task card · WP-27 formal pass · role A3 (Formalist)

**Objective:** discharge the four `[A3: formalise]` stubs in
`rehydrations/academic/conjecture_governance_method.md` (draft-v1/P0, L104)
and give the paper's formal statements theorem discipline. This is a METHODS
paper: the formal objects are definitions and invariants, not new theorems —
resist inventing mathematics the record does not carry (GR-8; the E10 claims
are process-record, and a formalisation that outruns the record is a
strengthening).

**The four stubs:**
1. **Gate-ladder state machine (§3.2):** define the ladder as a finite state
   machine over artifact states (draft, P0..P3, awaiting-P4, P4) with the
   transition relation, the roles licensed per transition, and the
   non-delegable gate as the unique transition whose licensed-role set is
   {human principal} by construction — a machine state no agent transition
   reaches. State the invariant the design enforces (no path to P4 that does
   not pass through the human), not properties of operation the record
   cannot show.
2. **Ledger append-only invariant (§3.3):** state the invariant precisely
   (entries totally ordered by index; no entry mutated or deleted after
   append; corrections are new entries referencing old indices) and state
   honestly that enforcement is PROCEDURAL (single serialising writer), not
   cryptographic — the Certificate Transparency contrast in §5 depends on
   this being said exactly (L104).
3. **Production-direction constraint (§3.4):** formalise the two-register
   discipline as a constraint on the citation/derivation graph (formal
   register statements may be sources of narrative register statements;
   narrative statements may not be sources of formal ones), with the dated
   qualification the draft already carries (the direction was itself earned,
   E10-C01's dating note) preserved, not erased.
4. **Register numbering invariants (§3.1):** YOUR RULING whether this folds
   into stub 2 (both are append-discipline invariants) or stands alone
   (numbering monotone; numbers never reused or renumbered; aliases retained
   where pinned artifacts cite them, E10-C20). Record the ruling and its
   reason in the chronicle.

**Also in scope:** a theorem-discipline sweep of the whole draft — any
sentence that reads as a formal claim must either be one of the defined
invariants, carry its [E10-Cnn] process-record basis, or be reworded to
observational strength. The transfer hypothesis (§7) keeps hypothesis shape:
grounds, falsification condition, no generality assertion.

**Checks:** four `checks/` scripts on the file with the explicit path; all
must PASS. Bump frontmatter to draft-v2.

**Out of scope:** prose voice (A2/A7 territory); the novelty section's fence
wording (L102-governed; touch only if a formalisation forces a consistency
edit, and record it); canon; manifest; ledger (return proposed entry to A0);
extractions.

**Definition of done:** draft-v2, stubs discharged, sweep done; check runs
recorded; chronicle per `chronicles/README.md`; proposed ledger entry
returned to A0.
