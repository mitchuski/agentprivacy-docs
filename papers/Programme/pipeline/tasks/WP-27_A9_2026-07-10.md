---
wp: WP-27 (methods-paper-conjecture-governance)
role: A9
runtime: fable
tier: A
branch: none (convention suspended)
gate_target: P3 (four checks green + consistency audit; on green WP-27 flips awaiting-P4)
due: 2026-07-10
---

# Task card · WP-27 P3 consistency station · role A9 (Consistency Auditor)

**Objective:** the P3 station on
`rehydrations/academic/conjecture_governance_method.md` (draft-v3 through
L109; P0/P1/P2 all on file). The WP-04/WP-07 station repeated for the third
paper. On green, A0 flips WP-27 to awaiting-P4 and the runtime touches it no
further.

**The station:**
1. Four deterministic checks on the explicit file; your run is the evidence
   of record (argv-less exits 2 by design). Also run them on
   `rehydrations/grants/grant_edition.md` (joined the sweep list at L101;
   this is its first P3-station coverage).
2. Internal consistency post-churn (the draft absorbed two revision seats
   same-day): Definitions 1-4 and invariants I1-I12 — numbering intact
   after the I9 weakening; every in-text invariant reference (I_A set in
   section 7, the enforcement remarks, section 5's pointer to the 3.3
   remark) resolves to an invariant that still says what the referencing
   sentence needs; the section-7 adoption criterion's per-invariant
   check-descriptions match the invariants as now worded; Definition 1's
   consolidation origin and section 3.4's temporal index are the same
   pattern and do not contradict.
3. The L105(c)/L107 probe class, re-run post-fix: the enforcement episodes
   in sections 3.1 and 4.4 now instantiate exactly what they are cited
   under (the authority-lag under I4/I2; the erratum under the register-wins
   prose rule; the 111/111 catch under the extraction sweep, not a
   coherence-instrument generation).
4. Trace map faithful: every [E10-Cnn] marker in the body appears in the
   trace map's section lines; the nothing-exceeds-STATUS line true; the
   consumed/excluded lists match the file (E10-C25 partial, E10-C31
   excluded); the added [E10-C04] at section 4.4 recorded.
5. Frontmatter vs manifest vs this card: status draft-v3, gate P1; register
   head C96 triple-agrees (manifest :4, check_register_refs.py HEAD,
   register file).
6. Count-structure audit (the WP-07 F1 class): any enumerations ("four
   components", "nine operating rules", "twelve invariants", "four
   generations", "two instances", "three episodes") investigated to ground
   truth in the file.

**Out of scope:** fixing anything found (findings loop to owners); canon;
manifest; ledger (return proposed entry to A0); tracker (A0 rebuilds at
close).

**Definition of done:** findings list (or explicit GREEN statement) with
file:line anchors; the probe-class re-run verdict stated separately;
chronicle per `chronicles/README.md`; proposed ledger entry returned to A0.
