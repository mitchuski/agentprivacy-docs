---
role: A1
runtime: fable
branch: none (convention suspended)
gate_target: E2 FEEDS hygiene (L081(c))
due: 2026-07-09
---

# Task card · E2 FEEDS reconciliation · role A1 (Extractor)

**Objective:** The L081(c) hygiene item: `extractions/E2-moving-ceiling.md`
claims E2-C02, E2-C04 and E2-C12 are consumed by WP-07 (its trace map and
frontmatter cite them) but their FEEDS lines do not name WP-07. Per the
L031/L065(d) discipline, FEEDS lines record actual consumption.

**The work:** (1) verify consumption: WP-07's trace map
(`rehydrations/academic/linear_cap_paper.md`, traceability appendix) and
frontmatter extraction_basis against each claim; (2) add WP-07 to the three
FEEDS lines (and ONLY where consumption is real — if one is not actually
consumed, report instead of editing); (3) re-run the four checks in
`checks/` WITH THE EXPLICIT FILE LIST (argv-less passes vacuously, L082) —
note the E1 check_versions FAIL is the standing L033 tension, record not
fix; (4) sweep for the same gap in the other direction while there: any E1
claim WP-04's trace map consumes whose FEEDS line lacks WP-04.

**Out of scope:** claim wording (the L074 E1-C10/E2-C12 descriptor re-issue
WAITS on the first person's register-brief item (e) ruling — do NOT touch
it); canon; manifest; ledger (proposed entry in report).

**Definition of done:** FEEDS lines reconciled; chronicle
`chronicles/2026-07-09_a1-e2-feeds-hygiene.md`; report to A0 + proposed
ledger entry (L0nn).
