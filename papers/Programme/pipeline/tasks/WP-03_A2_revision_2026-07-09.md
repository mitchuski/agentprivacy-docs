---
wp: WP-03 (whitepaper-grant-edition)
role: A2
runtime: fable
tier: G
branch: none (convention suspended)
gate_target: draft-v3 (all nine L100 findings resolved under the A0 rulings; A0 targeted re-check follows)
due: 2026-07
---

# Task card · WP-03 revision 1 · role A2 (Register Translator)

**Objective:** resolve all nine findings of the A5-assessor review 1 (memo on
`tasks/WP-03_A5_assessor_2026-07-09.md`; ledger L100) in
`rehydrations/grants/grant_edition.md`, draft-v2 → draft-v3, under the four
A0 programme-shape rulings recorded in L100. Fix to the memo's what-satisfies
lines; extend wording only in narrowing directions (the WP-04/WP-07 revision
discipline). No fix may strengthen any claim beyond its extraction STATUS.

**The four MAJORs, under their rulings (L100 verbatim governs):**
1. WP-H (:218): state the mechanisation floor — the section-2 proven core
   mechanised end to end with the axiom list published; below it the package
   reports failure; partiality counts only above it. Obstruction branch
   unchanged.
2. WP-C (:168) + WP-E (:188): windows = six months, two per funded period,
   null trigger tied to the renewal decision; define qualifying instance = a
   publicly verifiable event, verified under the WP-00 discipline, that moves
   a dated t* re-estimate earlier for a named archive class; the same rule
   governs WP-E's second-instance clause.
3. Independence (:91/:99, :146, :164): qualify the sentence; WP-A's erasure
   arm instantiates from the built record (the workflow-level harness
   instance per [E1-C38], its depth capability stated honestly; the
   two-process engine arm declared as the WP-F interface); WP-C funded alone
   uses a declared interim estimate (published capability results as the
   auditor's lower-estimate instrument) and inherits WP-B's methodology on
   delivery; state interfaces per package where they exist.
4. WP-G (:206): month-0 feasibility gate — securing an operating credential
   network under consent is the first milestone, with a stated failure
   deadline (3 months) reported as the package's negative outcome; name the
   candidate from the built record WITHOUT canon vocabulary (the deployed
   relationship-credential implementation of [E1-C36]'s design family, worded
   from the extraction).

**The five MINORs (memo what-satisfies lines govern):**
- MINOR-1 (:89): Zcash agency — "an auditor using a model released the
  previous day", matching :123 and the E2-C05 record.
- MINOR-2 (:182 + trace map :274): re-attach the corroboration model to where
  E2-C14 holds it (the convex profile), carry monotonicity clean, and correct
  the trace-map line so it records the attachment faithfully.
- MINOR-3 (:261): remove the Bergstra-Burgess reference line (unconsumed;
  E1-C17 is deliberately not consumed) or re-tag it accurately if a consuming
  passage exists — removal preferred.
- MINOR-4 (:148): give WP-A a decision rule (pre-registered discrimination
  criterion with the N-range and a power/tolerance statement of the same
  class WP-B and WP-G already carry).
- MINOR-5 (:91/:226): add locators — the public conjecture register named as
  the same public reference class as the specification URL; pre-registration
  platform named as OSF-class, fixed at adaptation.

**Checks:** four `checks/` scripts on the file with the explicit path after
editing; all must PASS.

**Out of scope:** structure of the package set beyond the rulings; canon;
manifest; ledger (return proposed entry to A0); other files.

**Definition of done:** draft-v3; per-finding resolution list keyed to the
memo's anchors; check runs recorded; chronicle per `chronicles/README.md`;
proposed ledger entry returned to A0.
