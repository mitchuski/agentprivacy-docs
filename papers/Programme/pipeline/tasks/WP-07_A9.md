---
wp: WP-07 (paper-one-theory, TIER-A, PoPETs)
role: A9
runtime: fable
tier: A
branch: none (convention suspended)
gate_target: P3 (four checks green + consistency pass) -> awaiting-P4
due: 2026-07-09
---

# Task card · WP-07 P3 consistency station · role A9 (Consistency Auditor)

**Objective:** The final runtime station for WP-07. The paper
`rehydrations/academic/linear_cap_paper.md` is at revision-draft-v2, gate P1
(L080: review-1 loop closed, targeted re-check passed x6). Run the P3
consistency pass; if green, WP-07 flips to awaiting-P4 and the runtime
touches it no further.

**The pass:**
1. The four deterministic checks in `checks/` — your run is the P3 evidence
   of record.
2. Internal consistency after the revision churn: theorem/proposition
   numbering (note Prop 5.5b is a deliberate non-renumbering insert, L078 —
   verify every cross-reference to 5.5b/5.6/5.7/5.8 resolves); "Open Problem
   5.8" naming consistent file-wide; the necessity trio (4.3/5.5/5.5b)
   counted as three everywhere it is counted; no surviving "Conjecture 5.8",
   no blanket "auditable" over the five ERs, no "even without erasure".
3. Trace map vs body: every claim marker resolves against
   `extractions/E1-amnesia-gap.md` / `extractions/E2-moving-ceiling.md`; the
   trace map's revision annotations (L069(b), L073, L076, revision-1 rows)
   match what the body carries.
4. Frontmatter coherence: status line, extraction_basis, bib pointer,
   gate_target/handoff lines vs manifest WP-07 (read-only — report any
   mismatch, A0 amends).
5. LaTeX sanity: the template mapping note names the target scaffold; confirm
   `templates/submission/references/pv_v6.bib` still resolves every citation
   key used in the draft (key-presence grep, not a compile).

**Out of scope:** fixing anything (findings loop to A0 for routing — at P3 a
finding blocks the flip); canon; manifest; ledger (proposed entry in your
report); tracker regeneration.

**Definition of done:** P3 verdict (GREEN -> flip recommended, or findings
blocking); chronicle `chronicles/2026-07-09_a9-wp07-p3.md`; report to A0 with
the evidence table + proposed ledger entry (L0nn).
