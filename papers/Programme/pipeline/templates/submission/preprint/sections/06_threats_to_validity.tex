% ----------------------------------------------------------------------------
% Threats to validity / limitations. This section is load-bearing, not
% boilerplate: every named precondition gets a paragraph on what happens
% when it fails. No bound is stated here without its preconditions and,
% where the model requires it, its time index, restated in the same passage.
% ----------------------------------------------------------------------------
\section{Threats to Validity}
\label{sec:threats}

\paragraph{Construct validity.}
\todo{Does the measured quantity operationalise the claimed one? Estimator
bias, proxy gaps.}

\paragraph{Internal validity.}
\todo{Confounds in the experimental design; sensitivity analyses run.}

\paragraph{External validity.}
\todo{Which deployment classes the results transfer to, and which they do
not. Adversary classes excluded by assumption.}

\paragraph{Assumption failure.}
\todo{For each numbered Assumption: the consequence of its violation,
stated quantitatively where possible (e.g.\ collusion between agents
collapses the conditional-independence residual that the bound relies on).}
