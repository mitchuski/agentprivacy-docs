% ----------------------------------------------------------------------------
% Model and preliminaries. Vocabulary is fixed programme-wide: boundary
% agent S, delegation agent M, source X, structural context erasure,
% conditional-independence residual. Define every symbol before use; state
% the adversary class and the non-collusion assumption explicitly; any
% ceiling or bound is time-indexed where the model requires it.
% ----------------------------------------------------------------------------
\section{Model and Preliminaries}
\label{sec:model}

\subsection{Notation}
\todo{Random variables, alphabets, entropy $\Ent(X)$, mutual information
$\MI(X;Y)$, conditioning conventions. Cite~\cite{cover2006elements} for
standard definitions rather than restating them.}

\subsection{System model}
\todo{Agents, channels, and composition. Name each architectural
precondition as a numbered Assumption so later theorems can reference it.}

\begin{assumption}[\todo{name}]
\label{ass:example}
\todo{State the precondition precisely, including the adversary class it
holds against.}
\end{assumption}

\subsection{Threat model}
\todo{Adversary capabilities, what is observable, what is excluded, and
why the exclusions are realistic for the deployment class studied.}
