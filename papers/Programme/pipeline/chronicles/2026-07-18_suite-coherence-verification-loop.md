# 2026-07-18 · The suite coherence verification loop, run 1

**Verdict first.** The execution-packet §5 verification recipe was run suite-wide for the first time since the G-REG batch executed, and it earned its keep: seven residues of the ruled soil batch had survived L151 and the display agent's waves. Six are closed this session as uncommitted working-tree edits under the L149/L150 ruling authority; four further contradiction sites are routed, not touched. Ledger L152.

## What the loop found and closed

1. `papers/v6/privacy_value_v6.md:62` — the mechanism sentence still said "grow as frontier capability grows". It escaped L151 because that session's verification grep matched only the phrase "frontier capability growth raises". Re-typed to the informational clock. Checklist B6 now fully closed.
2. `papers/v6/dualprivacy_researchpaper_v6.md` — never in the L151 sweep at all. §2.1 C82 sentence re-typed; §2.4 canonical-figures rule now carries the 678×/31,000× retirement.
3. Whitepaper `:577` — the second clause still asserted the 31,000× figure as current understanding; re-typed to fenced lineage. Checklist B1 now fully closed.
4. Whitepaper `:675/:1447/:1641/:1859` — four compound-near-value residues scoped to the reputational channel per the ruled C55 row.
5. `models/privacy_value_model_v6{,_light,_dark}.json` — the docs repo's own model JSONs were two rulings behind: head C96, pre-ruling C82, figures asserted, `next_free: C97` after C97 was minted. All three synced: head C97, C55/C82 re-typed, Band XI row, figures retired, version-log 6.2 entry. BOMs preserved, JSON validated.
6. `models/privacymage_grimoire_v10_4_0.json` — a fourth live v10.4 grimoire home the three-home spell disposition missed. Diff against master's ruled canonical showed exactly the three disposition lines; the docs copy is now byte-identical to the canonical.
7. `rehydrations/academic/moving_ceiling_sok.md` Conjecture 1 — the registered-wording update L149 released. Re-typed to the ruled row; proof obligation split proven-form / conjectural-rate per Cor 5.4b. All four checks PASS.

## Routed, not touched

- master `public/models/privacy-value-model-v6.json` at head **C93** (two rulings behind its `src/data` sibling) — display-agent/build-step territory.
- master register mirror `:704` — Mitch's regen script (classifier-blocked, known).
- master `public/story/24-act-xxiv:409` asserts the multiples — cityofmages/Tome register pass.
- Pipeline `tracker/`/`reader/` HTML + `blog/reader/post1.html` — stale generated surfaces; regenerate, never hand-edit (GR-6).
- WP-01 essay `:43` "drag the frontier growth rate" — describes the instrument's pre-ruling slider label; P4-adjacent, artifact awaiting-P4, flag only.

## Reversals

None. One method correction recorded: the packet §5 grep patterns were narrower than the wording class they fence; the widened patterns (`frontier capabilit|frontier capacity|frontier growth|grow as frontier|grow with frontier`) should replace them on the next run.

## Handoff

- **First Person:** review the working-tree diffs together with the L151 batch; commits are yours. P4 reads unchanged (WP-01, WP-04, WP-07 rev-v3, WP-14 draft-v4, WP-27). Mirror regen script + deploy train + fedwiki secret rotation still pending.
- **Display agent:** master `public/models` JSON head C93 flagged; story act XXIV multiples ride the Tome register pass.
- **Runtime, next:** this loop is WP-13 (suite-consistency audit, A9, weekly) in all but name — run weekly against the register head. WP-14 remainder: A4 full P2 cite pass + interleave; A13 CFP check 2026-11-01. Extraction frontier: E3/E5/E6/E8/E11 pending, E2 full widening sweep open. Targeted A5 spot-look at SoK Conjecture 1 at next review touch (L147(c)).
