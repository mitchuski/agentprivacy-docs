---
date: 2026-07-10
role: A7
wps: [BLOG/T-arc (pool series, post 1)]
extractions: []
register_head: C96
ledger_entries: []
---

# Chronicle · A7 · pool-series post 1, method-only draft

## Verdict

Draft-v1 of the method-only public post landed at
`rehydrations/public/two_agents_walk_into_a_circuit.md` (~1,300 words body),
inside the L116 post-1 fence, with all four `checks/` scripts PASS on the
explicit path and the fence-compliance apparatus carried as an HTML comment
in the file. No target project, no findings, no workshop numbers, no
announcement of the staged public event. The seed's liftable thesis
paragraph was adapted with all four target-domain particulars stripped. The
post ends on the rung-1 consent-first invitation (take the pattern; declining
costs nothing). A title alternative is proposed to A0 rather than applied;
the file keeps its carded name.

## Path

1. Boot per the pipeline CLAUDE.md: GROUND_RULES (GR-5, GR-2 TIER-P row,
   GR-8), role card A7, task card BLOG_A7_pool_post1_2026-07-10, then
   sources: the blog seed (L116 intake), the dual-agent harness brief of
   2026-07-09 (main source), the two arc voice exemplars, and the ledger
   tail through L116.
2. Read all four check scripts BEFORE drafting (the L113 ordering lesson):
   confirmed the em-dash arm fires at TIER-P, the version-field pattern
   requires a non-digit-leading version string, and the argv-less guard
   exists. Frontmatter was written to the exemplars' conventions
   accordingly.
3. Content selection. In-fence spine: non-collusion of the two roles;
   test points hashed from the proposal (the challenge-derivation move
   carried as method, not cryptography); pre-registered verdict rules with
   "cannot decide" filed as such; negative results and the killed-levers
   census as the precondition for closure certificates; the vacuous-checks
   episode told at depth as the anti-correlated-detector war story; the
   two-instances-converging observation at the harness brief's own section
   10 wording; the human-only hard stop. Out: everything the fence names.
4. Checks: 4/4 PASS first run on the explicit path. Post-draft sweeps
   (em-dash grep, American-spelling grep, fence-word grep, tail-byte
   inspection) run in addition to the scripts.

## Reversals

- REVERSAL 1 (fence self-catch): the first apparatus comment named the
  stripped particulars verbatim ("pool, withdraw-circuit, constraint-count,
  ceremony"), which would let a source-reader reconstruct the target domain
  from the shipped file. The hard rule ("if a sentence would make a reader
  able to identify the target project, cut it") outranks apparatus
  completeness. Rewritten to name the omissions by category only, with the
  reason stated in the comment itself.
- REVERSAL 2 (seal placement): the apparatus comment was first placed after
  the closing 🙂, violating GR-5's "🙂 at literal end". Moved to the top of
  the body, directly after the frontmatter. Tail bytes verified: the file
  ends 🙂ǀnewline.
- Two UK-spelling touches after first write ("artifacts" to "artefacts",
  twice); the `license:` frontmatter field name kept as the exemplars have
  it.
- Overlap decision, recorded as a deliberate omission rather than a
  reversal: the reviewer-found-a-false-theorem story from the harness brief
  was left out because the moving ceiling essay's coda already carries it;
  this post's war story is the vacuous-checks episode instead. The residual
  overlap (both pieces mention vacuous checks; the coda in one sentence,
  this post at depth) is a posting-order consideration for the first
  person, noted in the A7 report, not resolvable at this seat.

## A7 checklist (role card, item by item)

- zero em-dashes: PASS (grep count 0 over the whole file).
- UK spelling: PASS (optimisation, artefact, recognise, summarised; no
  American forms found by sweep).
- verdict-first opening: PASS (lowercase verdict line in the second
  breath).
- short spaced paragraphs: PASS.
- honesty labels with register-matching treatment: one speculative claim in
  the post (the template transfers beyond the two instances); labelled
  "argued, not measured, and carries no register number yet" on the
  competence_without_history precedent for unregistered claims; no invented
  percentage. Everything else is what-we-did reportage.
- lowercase proverbs and closing lines: PASS (four proverb lines, two
  closing lines, all lowercase).
- seal placement: PASS, `(⚔️⊥⿻⊥🧙)😊` then 🙂 at literal end.
- overlap check, posts compared: The Last Premine (published; no overlap,
  different subject), the_moving_ceiling.md coda and the_uncarved_date.md
  and competence_without_history.md (drafts on disk; overlap risk confined
  to the coda's one-sentence vacuous-checks mention and its dual-agent
  paragraph; this post neither retells the coda's paper-ladder story nor
  references any unpublished essay's status).

## Check runs of record

Invocation, from `checks/`, each with the explicit path
`C:\Users\mitch\agentprivacy-docs\papers\Programme\pipeline\rehydrations\public\two_agents_walk_into_a_circuit.md`:
check_register_refs PASS exit 0 · check_tier_vocab PASS exit 0 ·
check_figures_fence PASS exit 0 · check_versions PASS exit 0 (final run
after all edits; earlier identical 4/4 PASS runs preceded the apparatus
relocation).

## Handoff

- BLOG post 1: draft-v1 on disk; single next action = A0 registers the
  series in the blog frontmatter conventions and queues the draft for the
  first person's spin and posting (the final gate; the runtime does not
  post).
- Title: file name kept per card; alternative proposed in the A7 report
  ("Not Allowed to Collude") for the first person to take or leave.
- Open question for the first person: posting order relative to the moving
  ceiling essay, given the confined coda overlap noted above.
- Blocked: posts 2+ (their doors are first-person acts per L116; no cards
  exist and none requested).
- Proposed ledger entry returned to A0 in the seat report; nothing appended
  to the ledger from this seat.
