---
date: 2026-07-10
role: A9
wps: [WP-27, WP-03]
extractions: [E10]
register_head: C96
ledger_entries: []
---

# A9 · WP-27 P3 consistency station · conjecture_governance_method.md at draft-v3 (+ grant_edition.md first check coverage)

## Verdict

**P3 FINDINGS, not green. Four body findings, all MINOR, all in the count-structure and cross-reference classes; zero apparatus-of-claim defects; the L105(c)/L107 probe class re-runs PASS on all four episodes.** The six-item pass over `rehydrations/academic/conjecture_governance_method.md` (draft-v3 through L109) found the WP-07 F1 class alive in two places (a "four further instruments" count over five instruments; a four-generation parenthetical that misaligns with E10-C24's generation boundaries), one blanket claim ("three episodes are already reported above" where the third is first reported in place), and one directional cross-reference error ("below" for a rule stated above). None widens a claim; none touches the novelty fence; the definitions, invariants, adoption criterion, temporal indices, and trace-map primary direction are all clean. Two trace-map phantom listings and two stale frontmatter fields are reported separately as apparatus, the WP-04 L093 class. The four deterministic checks PASS non-vacuously on the explicit two-file list and suite-wide (nine files); `grant_edition.md` receives its first P3-station check coverage, 4/4 PASS. No fix was applied at this seat (GR-6; P3 discipline); findings loop to owners via A0. A P3 with findings is the station working; the flip to awaiting-P4 waits on the four one-sentence repairs and a targeted re-run.

## Evidence table (six-item pass per task card)

| Item | Scope | Method | Result |
|---|---|---|---|
| 1. Four checks | tier_vocab, figures_fence, register_refs, versions on the explicit two-file list (conjecture_governance_method.md, grant_edition.md), then suite-wide over all 9 rehydration files; argv-less guard probed | checks/ scripts, explicit paths as argv (L082 discipline) | PASS x4 on the two named files (exit 0, 2 PASS lines each); PASS x4 suite-wide (exit 0, 9 PASS lines each, non-vacuous); argv-less invocation exits 2 by design. grant_edition.md first-station coverage on file |
| 2. Internal consistency post-churn | Definitions 1-4, I1-I12, every in-text invariant reference, section-7 I_A descriptions, Definition 1 origin vs 3.4 temporal index | grep sweep of all I-references + line reads | Definitions 1 (:271), 2 (:297), 3 (:315), 4 (:335); I1-I12 stated once each, sequential, numbering intact after the I9 weakening (:323 reads as exactly E10-C05 content with the no-further-semantics clause). Every in-text invariant reference resolves: :283 (I4 with I2; I1; the register-wins rule named as prose Definition 1 does not contain), :285 remark (I7/Section 3.3, I5/Section 3.2), :325 (I7 to I9), :343 (I4; the temporal index below, which is at :345, correctly below), :437-:441 (I_A = {I1,I2,I4,I5,I6,I7,I8,I10,I11}; exclusions I3/I9/I12 reasoned in-text; per-invariant check descriptions match the invariants as now worded, including I9's exclusion matching the weakened I9's observational subsumption by the I7/I8 history diff), Section 5 :401 pointer to the 3.3 enforcement remark resolves and the remark reciprocates. Definition 1's consolidation origin (:271) and Section 3.4's temporal index (:345) are explicitly the same pattern and do not contradict: each fixes a dated origin, places the earlier practice outside the indexed scope, and keeps the earlier failure on the record (:265, :333). ONE finding here: F4, the :325 "contemporaneity rule below" points down at a rule stated above (:313) |
| 3. Probe class re-run (L105(c)/L107) | the four enforcement episodes in 3.1 and 4.4 vs what they are cited under | line reads vs E10-C12/C17/C24/C29 wording | PASS x4. Authority-lag (:269, cited :283) instantiates I4's licensing boundary with I2's registration-only rule: numbers carried ahead of the register, caught, routed to the human principal, not absorbed. Visible-erratum (:269, cited :283) cited under the register-wins rule as prose governance Definition 1 does not contain, matching E10-C12's prose rule and E10-C17's second earning event; no invariant claimed. The 111/111 catch (:387) attributed to the extraction sweep under trace-or-delete [E10-C24][E10-C04]; generation four keeps E10-C24's denotation ("deterministic checks proven non-vacuous"); quoted fragments verbatim against E10 :187 ("stood unnoticed in the corpus until this sweep") |
| 4. Trace map fidelity | every body [E10-Cnn] marker vs map section lines; nothing-exceeds-STATUS; consumed/excluded lists; the added E10-C04 | scripted marker inventory (30 distinct markers, all occurrences by line) vs :472-:488 | Primary direction PASS: every body marker appears in its section's map line, all sections. Consumed/excluded lists match the file: E10-C25 partial (occurrences :333/:343/:345/:423, all dating evidence), E10-C31 zero occurrences, remaining 29 claims all present. [E10-C04] at Section 4.4 recorded (:485) with the no-extraction-claim note for the revision-record sentence. Nothing-exceeds-STATUS line true in its own terms (no rule asserted optimal/complete/sufficient; earned-twice claims carry both events: C06/C07/C08/C11 at 2.2+4.2, C12/C17 both episodes at 3.1; workshop at claim level, fence stated :367; quotes verified verbatim against E10 carried wording). TWO apparatus findings: the Section 1 map line (:473) lists E10-C13 with no C13 marker in Section 1 (C13's nearest occurrence is the Section 2 preamble :225, covered by the 2.1 line); the Section 7 map line (:488) lists E10-C07 with no C07 marker in Section 7 (the hypothesis grounds on C15+C06+C08+C11 only, :435, correctly, since prediction (i)-(iii) omit the interruption rule) |
| 5. Frontmatter vs manifest vs card | status, gate, register head triple-agreement | line comparison, manifest read-only | status draft-v3 (:6) = manifest WP-27 row (:56) = task card. Manifest gate P1 = card. Register head TRIPLE-AGREES: manifest :4 C96; check_register_refs.py :7 HEAD = 96; register file :5 "Register head: C96". TWO stale frontmatter fields for A0 (below), the WP-04 L093 class |
| 6. Count-structure audit (WP-07 F1 class) | every enumeration investigated to ground truth | count vs file and vs E10 | Clean: four components (:199, :227 = 4 bold entries), twelve invariants (I1-I12 complete), four operating rules earned twice (:219 = 4 bullets at :373-:379), two instances (:365-:367), three same-day loops with per-instance finding counts matching E10-C09 exactly (:383), at least five interruption events (:375 = E10-C07), six gates signed (:291 = E10-C03), four collision clusters (:265 = E10-C12), eighteen days (:293 = E10-C19), three named referee risks (:425 = the prior-art review's limits: problem-list, ELN, philosophy-of-science), 9 + 3 = 12 in the adoption criterion (:437), 111/111 carried as unreliable. NOT clean: F1 (:349 "Four further instruments" over five bold instruments :351-:359) and F2 (:387 generation parenthetical vs E10-C24) and F3 (:387 "Three episodes are already reported above") |

Role sweep-list extras, this run: retired citations (Research Paper v4.0/v4.2) zero in both files; static-ceiling sentence family zero (no R < 1 site exists in this paper); canonical figures and fiat figures zero (fence check PASS); no emoji in either file; no em-dash in either file (TIER-A body and TIER-G both clean); UK spelling spot-checked clean in the body.

## Findings (body; loop to owners via A0; no fix applied at this seat)

1. **F1 · :349 · count-structure (the WP-07 F1 class).** "Four further instruments recur in the record" introduces five bold instrument paragraphs: Resumable driving documents [E10-C18] :351, Dated verdict-first plans [E10-C21] :353, Origin-first reflection [E10-C22] :355, Read-only standing surveys [E10-C29] :357, Built-never-hand-edited structured data [E10-C30] :359. One-word fix (owner A2). MINOR.
2. **F2 · :387 · trace fidelity under [E10-C24] (GR-9).** The four-generation parenthetical "(hand-ticked propagation checklists; review-only audits with priority tiers; per-document convergence studies and coherence reports with honest gap tables; deterministic checks proven non-vacuous)" misaligns with E10-C24's generation boundaries: its items two and three are both components of the extraction's generation two (which also includes grep-based checkpoints), and the extraction's generation three (the V6 plan's coherence-by-hand-stopped-scaling verdict plus the deterministic audit-tool build spec whose acceptance test is reproducing known findings) is omitted entirely. The count "four" is correct and generation four's denotation (the L107 MAJOR-1 fix) is intact; the misalignment is in the middle generations, inside the paper's flagship honesty episode. One-sentence re-enumeration (owner A2, wording checked against E10 :187). MINOR.
3. **F3 · :387 · blanket claim.** "Three episodes are already reported above and are gathered here deliberately": the first (vacuous-check, Sections 1 and 2.2) and second (strengthening, Section 2.2) are reported above; the third (the instrument-generation record) is first reported in Section 4.4 itself. Narrowing fix, e.g. "Two are reported above; a third is added here" or "Three episodes are gathered here" (owner A2). MINOR.
4. **F4 · :325 · cross-reference direction.** The enforcement remark cites "the contemporaneity rule below [E10-C27]"; the rule is stated above, at :313. Likely residue of paragraph ordering at the formal pass. One-word fix (owner A3, whose remark it is, or A0 micro-touch per the L082 precedent). MINOR.

No finding touches the novelty fence (N1-N4/S1-S6 wording verified untouched at fence width), no claim is widened, and nothing here reopens the L107 MAJORs, all four of which were verified in place this run.

## Apparatus findings (not body; A0's queue)

1. Trace map :473 (Section 1 line) lists E10-C13; no [E10-C13] marker exists in Section 1 (:201-:207). Phantom or draft-v1 residue.
2. Trace map :488 (Section 7 line) lists E10-C07; no [E10-C07] marker exists in Section 7, and the body is right to omit it (predictions (i)-(iii) do not include the interruption rule).
3. Frontmatter `gate_target` (:9) is stale: the L109 re-check executed and P1 is marked; the current station is P3, target awaiting-P4. Frontmatter `handoff` (:10) "A4 (bibliography resolution...)" is stale: A4 completed at L106. Same administrative class as WP-04's two fields at the L093 station.
4. Non-blocking observation: the handoff comment's "STILL OPEN FOR DOWNSTREAM SEATS" block (:76-:88) is dated by the A4 and revision addenda below it; dated record, stripped at release, no action before the port.

## Checked and cleared (so the reasoning survives)

The Section 7 adoption criterion was the closest candidate to an internal contradiction post-churn and was cleared on wording, not assumption: its I1 description ("every number denotes one claim across all states") does not collide with the revised I1's indexed-history reading, because an adopting loop's register history begins at its own origin, so "all states" and "the indexed history" coincide for the adopter; its I9 exclusion ("the same history diff that checks I7 and I8") matches the weakened I9 exactly, which now adds only the designated-mutable-field clause and no further semantics; its I12 exclusion matches I12's own closing composition sentence. The two temporal indices (Definition 1's consolidation origin; Section 3.4's mid-2026 scope) were checked as the same pattern and for mutual contradiction: none; both keep the excluded earlier practice on the record rather than erasing it. The "three named referee risks" count (:425) was verified against reviews/WP-27_prior_art.md's limits (problem-list, ELN, philosophy-of-science framing). Verbatim quotes were checked one by one against E10's carried wording: "an audit trail that never admits error is evidence of nothing", "a session without a chronicle is unfinished", "and the evidence is this directory", "as if a methods examiner will read it, because one will", "which is what makes them evidence", "because it was there", "so it cannot flatter", "a dead end named is a contribution; a dead end omitted is a defect", "the proposer does not approve its own proposal", "nothing publishes unread", "no emergence", "Two domains, one discipline, convergent rules", "a lag to close, not a contradiction to arbitrate". All match.

## Reversals

None withdrawn after being raised. One candidate finding was investigated and NOT filed: the paper's "stale-still" (:357) inverts E10-C29's "STILL stale", but the paper does not quote the phrase, so it is a paraphrase, not a verbatim-quote defect. No check invocation was vacuous this run (explicit file lists throughout; PASS-line counts confirmed per invocation; the argv-less guard separately probed and observed exiting 2).

## Handoff

- **WP-27, single next action:** A0 routes F1-F3 to A2 and F4 to A3 (or executes F1/F3/F4 as micro-touches per the L082/L106 precedent, F2 with E10 :187 open), amends the two frontmatter fields, appends the ledger entry, then a targeted re-run of this station's items 3, 4, and 6 on the four anchors (:325, :349, :387) plus a fresh 4-check run. On green, A0 flips WP-27 to awaiting-P4 and the runtime touches it no further.
- **WP-03 (grant edition):** first P3-station check coverage on file, 4/4 PASS, nothing owed; it stays at its own chain's state (release-draft-v3, P1).
- **Open items riding elsewhere:** the two trace-map phantom listings ride with the F-fixes (same file, same owner pass); the dated handoff-comment block needs no action before the LaTeX port.
- **Blocked:** nothing.
