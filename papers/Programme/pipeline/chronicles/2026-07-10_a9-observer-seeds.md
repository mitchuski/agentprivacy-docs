---
date: 2026-07-10
role: A9
wps: [WP-13]
extractions: []
register_head: C96
ledger_entries: []
---

# A9 observer regression-seed conversion · cycle 11

## Verdict

The eight OBS-GPT56 regression seeds RS-01..RS-08 are converted into pipeline instruments at reviews/OBSERVER_REGRESSION_SEEDS.md, completing the protocol section 6 criterion ("all regression seeds are written as check-script cases or A9 cards") for the intake run on the pipeline side. Disposition: one new check-script case proposed (RS-01, a HISTORICAL arm for check_versions with both-direction probes on the L091(d) model), one minor check-script extension proposed riding it (RS-03, the GR-7 retired sentence family as an unconditional pattern), four A9 audit-card items with evidence forms (RS-04 hash-as-identity census, RS-05 scope-honesty census, RS-07 ceremony-as-proof census on TIER-P/G, RS-08 the two-map decision procedure in full, three questions), and three seeds already covered by existing discipline, cross-referenced rather than duplicated (RS-02 by GR-9 and protocol rule 8 with one named sweep clause added; RS-03 by GR-2/GR-7 and the conditioning arm; RS-06 by the weekly manifest-versus-frontmatter compare, the register-head triple-agree, and the check_register_refs HEAD arm). No check script was modified; both proposed cases await a checks-maintenance card per the session sanction (propose, do not implement). The seeds file itself passes all four checks, 4/4 non-vacuous, exit 0, after one self-caught defect recorded below; the argv-less guard was probed and holds (exit 2, loud refusal).

Session constraints from A0, honoured: writes confined to the seeds file and this chronicle; canon read-only; no manifest, ledger, or checks/ edits; no git. The ledger entry below is proposed and returned to A0, not filed.

## Path

1. Boot per CLAUDE.md: GROUND_RULES.md, the A9 role card, OBSERVER_INTAKE_PROTOCOL.md (rule 10 and section 6 as charter), OBS-GPT56_DISPOSITION_LEDGER.md section 3, and L121 read in order; role, writes, and definition of done stated before touching anything.
2. Instrument survey before design: the four check scripts read in full to establish which arms already own which trap (the RETIRED and conditioning arms of check_versions; the HEAD and tier arms of check_register_refs; the mythos, emoji, confidence-band, and em-dash arms of check_tier_vocab; the figures fence). The L091/L095/L098 ledger thread read to fix the synthetic-probe model (probe outside the tree, must FAIL with exactly one finding, both directions where applicable, then deleted; run ledgered). The cycle-9 weekly chronicle read for the audit-card evidence form.
3. Design rule adopted up front, from the OBS-GPT56-017 disposition: where existing discipline owns a trap, cross-reference and add at most a named sweep clause; two label systems for one trap is itself a drift generator. This ruled the RS-02/RS-03/RS-06 dispositions.
4. The seeds file written with a declared sweep-hygiene posture: fenced phrases appear only as regex source (which does not match itself), under in-paragraph conditioning, or as description; the GR-7 retired sentence and any above-head C-number are deliberately not reproduced.
5. Verification: all four checks run over the seeds file, plus the argv-less guard probe.

## Reversals and self-caught defects (GR-8 applies to the path record)

1. **RS-04 and RS-07 were first sketched as check-script cases and reversed to audit-card items.** The co-occurrence patterns are greppable, but the disposition of each hit needs the proof-qualifier judgment (integrity versus authorship; ceremonial versus cryptographic); a script would either miss qualified uses or flood the report. The greppable pattern is retained inside the audit step, so the census stays mechanical and only the classification stays with the auditor.
2. **The seeds file failed its own first check run.** check_versions flagged line 29: the RS-01 section quoted the retired research-paper citation pattern in matchable form, exactly the self-tripping class the file's hygiene note promised to avoid. Rephrased to non-matching description at this seat (the seeds file is this session's writable artefact, so the fix is not laundering); re-run 4/4 PASS. Recorded because it is evidence for the design choice: instruments that describe traps must be written trap-aware, and the RS-03 proposal already carries the same lesson (the retired sentence family is specified but not reproduced).
3. **A tier: key was considered for the seeds frontmatter and declined.** The file is apparatus, not a rehydration; the cycle-9 F3 rule question (tier-less frontmatter and manifest classification) is still open with A0, and pre-empting it here would fork the convention. Recorded as a dependency, not a defect.

## Proposed ledger entry (returned to A0, not filed)

[Lnnn][OBSERVERS][seeds][2026-07-10][A9 (regression-seed conversion per L121(b); serialised by A0)]
FINDING: reviews/OBSERVER_REGRESSION_SEEDS.md WRITTEN, the pipeline instrument for RS-01..RS-08 per protocol rule 10; the protocol section 6 criterion "all regression seeds are written as check-script cases or A9 cards" is MET on the pipeline side. Dispositions: RS-01 CHECK-SCRIPT CASE PROPOSED (check_versions HISTORICAL arm seeded with the spellbook repository identifier, fed from SOURCES.md historical/superseded flags, paragraph-window rescue; FAIL probe = current-source sentence, PASS probe = qualified provenance sentence; L091(d) model both directions); RS-02 ALREADY COVERED (GR-9 + protocol rule 8; one named clause added to the weekly trace-map step: no source list, prior-art row, or citation resolves to a generated artefact; evidence = one-line census); RS-03 ALREADY COVERED (GR-2/GR-7 + the conditioning arm and its verified probe) plus a minor RETIRED-class pattern PROPOSED (the GR-7 retired sentence family, currently rule-only, not script-policed); RS-04 AUDIT CARD (hash-class x proof-class co-occurrence census, per-hit integrity-vs-authorship classification); RS-05 AUDIT CARD (scope-honesty census, three clauses: verdicts name enumerations; totalising claims grounded; extraction coverage at stated depth, E10/L103 fence respected) with live precedent /model over-weighting (OBS-GPT56-005); RS-06 ALREADY COVERED (weekly manifest/frontmatter/tracker compare + head triple-agree + check_register_refs HEAD arm; the L098-F1 executed class as the working precedent) with live precedents the stale-numbering class and the 96/64 skill surface (OBS-GPT56-014, canon-side patch on the Section-5 ruling); RS-07 AUDIT CARD on TIER-P/G (ceremony-class x proof-class census, ceremonial qualifier as classification key), TIER-S/A structurally covered by the GR-4 arms; RS-08 AUDIT CARD, the paired trap, TWO-MAP DECISION PROCEDURE written out (Q1 both-sides-evidence-bearing -> canon map always; Q2 canonical-source-decides -> canon map, mystery is never version authority; Q3 intent-on-record -> route map, else canon map DEFERRED naming the human gate, intent is Mitchell's per protocol section 5; every drift finding henceforth carries map tag + deciding question). Instrument verified: 4/4 checks PASS non-vacuous on the seeds file, argv-less guard exit 2; one self-caught defect (the file's first draft tripped the RETIRED arm by quoting the pattern matchably; rephrased at the seat, re-run green) recorded in the chronicle. NO checks/ edit made.
EVIDENCE: reviews/OBSERVER_REGRESSION_SEEDS.md (disposition table + adoption path); chronicles/2026-07-10_a9-observer-seeds.md (reversals incl. the RS-04/RS-07 check-case-to-audit-card reversal); the check runs of record at this seat; OBSERVER_INTAKE_PROTOCOL.md rules 6/10 + section 6; OBS-GPT56_DISPOSITION_LEDGER.md section 3; L121(b).
PROPOSED: (a) A0 adopts the four audit-card items + the RS-02 clause onto the next weekly A9 card (seeds cited by RS id; evidence forms as specified); (b) a checks-maintenance card is CUT for the two proposed cases (RS-01 HISTORICAL arm; RS-03 retired-sentence pattern), A9 implements on ruling with both-direction probes ledgered; (c) the RS-08 map-tag + deciding-question fields become required on every drift-shaped ledger finding from adoption forward; (d) the seeds file joins the weekly sweep file list (it is check-clean by construction and self-tests the discipline); (e) cross-reference to the cycle-9 F3 rule question: the seeds file carries no tier key by the same open convention, one ruling should settle both.
STATUS: proposed(returned to A0 by A9; A9 filed nothing)

## Handoff

- **Open questions:** whether A0 rules the checks-maintenance card in (both proposed cases, or RS-01 only); whether the RS-08 map-tag fields become mandatory ledger form or recommended practice; the tier-key convention for apparatus files (rides the cycle-9 F3 rule question).
- **Blocked items:** the two check-script cases are blocked on the checks-maintenance ruling by design (propose, do not implement). The RS-06 canon-side patch (96/64 skill surface) remains blocked on Mitchell's Section-5 ruling item 5; out of pipeline scope.
- **Next actions per WP touched:** WP-13: A0 adopts the audit items onto the next weekly card and adds the seeds file to the sweep list; on a checks-maintenance ruling, A9 implements the two cases with both-direction probes and ledgers the run.
