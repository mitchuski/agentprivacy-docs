---
date: 2026-07-10
role: A3
wps: [WP-27]
extractions: [E10]
register_head: C96
ledger_entries: []
---

# A3 · WP-27 revision 1, formal legs (L107)

## Verdict

The five A3-routed legs of the L107 revision loop are discharged in
`rehydrations/academic/conjecture_governance_method.md`: MAJOR-2 executed
under the A0 WEAKEN ruling (I9's unextracted licensing clause removed, not
defended), MINOR-1 (the :206 citing sentence split so each episode is cited
under the rule it exercises), MINOR-2 (Definition 1 given a temporal origin
at the register's consolidation gate, on Definition 4's pattern), MINOR-5
(I12's narrative-sourced channel narrowed to dated design status), and the
MAJOR-3 support block (an invariant-named adoption criterion and a
method-native period operationalisation, placed as an `[A2: place in
section 7]` comment for A2 to consume). All four `checks/` scripts PASS on
the explicit path. Frontmatter status not bumped; A2's anchors untouched;
a proposed ledger entry is returned to A0, not filed.

## The legs

### MAJOR-2 (I9, :246; A0 ruling: WEAKEN, not widen)

Before:

> **I9 (status as the one mutable projection).** S_i is the only field of
> an appended entry that may change after append, and a change to S_i is
> licensed only by a later append that grounds it and is referenced by it,
> so the status history is itself carried in the appended sequence.

After:

> **I9 (status as the designated mutable field).** Of the four fixed
> fields of an appended entry, S_i is the designated mutable field: it
> alone may change after append, the core projection core(e_i) being
> immutable per I7. Corrections to the record are themselves filed as
> entries per I8; the record specifies no status-mutation semantics beyond
> this designation.

The removed clause ("licensed only by a later append that grounds it and
is referenced by it") and its consequence ("the status history is itself
carried in the appended sequence") have no E10-C05 basis: the extraction
states the fixed four-field form, the single serialising writer, and
corrections-as-entries, and no status-mutation semantics. The replacement
carries exactly the memo's what-satisfies content plus one explicitly
narrowing sentence stating the absence. A mechanical sweep ("status
history", "licensed only by a later append", "mutable projection")
confirmed no other passage leaned on the clause; the Section 3.3
enforcement remark and the Section 5 contrast cite the append-only
discipline and the enforcement class, neither the licensing semantics.

### MINOR-1 (:206)

The single sentence citing both enforcement episodes under "Enforcement of
I1 to I4" is split. The authority-lag episode stays under the invariants:
it is I4's licensing boundary in use (the corrective text drafted, the
minting act routed to the human principal, not applied), together with
I2's registration-only extension of dom(ρ), matching the probe's PASS
reading. The visible-erratum episode is now cited as an instance of the
register-wins rule stated in the section's prose, named explicitly as
prose governance that Definition 1 does not contain. No new invariant was
minted: E10-C12 states the register-wins/erratum rule as a prose rule of
register authority and E10-C17 carries the erratum instance as a
correction-flow rule; neither states a numbering invariant, so extending
Definition 1 would exceed the extraction (the alternative route the memo
left to A3's judgement, declined on that ground).

### MINOR-2 (:198 against :188)

Definition 1's index t now has a stated origin: the signed consolidation
gate at which the collision dispositions were confirmed, with the earlier
ad-hoc numbering practice explicitly outside the indexed history, citing
E10-C12 (the consolidation run and signed gate) and E10-C21 (the
twice-failed ad-hoc renumbering that motivated consolidation). I1's
"taken over the register's whole history" now reads "whole indexed
history (from the consolidation origin of Definition 1)". This mirrors
Section 3.4's temporal-index pattern, as the memo instructed, and is a
pure narrowing: the invariants are asserted over less history, and the
pre-consolidation collisions the prose reports no longer falsify I1.

### MINOR-5 (:266)

I12 is reworded on both memo-admissible axes, and both are narrowings.
The channel is stated as the same candidate-adoption routing that governs
conjecture-shaped residue from any non-formal source (E10-C12, matching
Section 3.1's prose), and its evidence is dated: the recorded instances
of a narrative unit introducing candidates lie in the earlier flow the
temporal index excludes (E10-C01/C25), the in-scope candidate instances
on the record are agent-work residue (E10-C12), and within the dated
scope the narrative-sourced case is stated at design status, "licensed
... and not yet exercised". No recent exercise of the channel was
manufactured. The E10-C12 marker added in Section 3.4 is recorded in the
trace map's Section 3.4 line, updated to match (the map must stay true to
the markers; leaving it stale would recreate the class of defect MAJOR-1
names).

### MAJOR-3 support (:356)

Two clauses drafted in an `[A2: place in section 7]` comment block at the
end of Section 7, per the card; Section 7's prose itself untouched (A2's
leg).

**Adoption clause.** "Operates the components as specified" is defined as
satisfaction of the invariant subset I_A = {I1, I2, I4, I5, I6, I7, I8,
I10, I11}, with a per-invariant statement of what in the adopting team's
own record checks it. Rationale for the subset:

- The criterion must be checkable from the adopting team's record alone
  (the memo's own requirement), so each included invariant is one whose
  satisfaction or violation is decidable from record artifacts the four
  components already force the adopter to keep: the register file history
  (I1, I2, I4), the governing documents plus gate record (I5, I6), the
  ledger sequence (I7, I8), and the derivation and citation relations
  (I10, I11). Every component of Section 2.1 contributes at least two
  invariants, so the criterion covers the whole assembly, not one
  instrument.
- I3 is excluded because its antecedent (external citation of a number by
  a pinned artifact) may never fire in a new loop's first operating
  period; a vacuously satisfied conditional cannot evidence adoption, and
  including it would let "as specified" be claimed partly on an
  unexercised clause, which is the vacuous-check defect class in
  miniature.
- I9 is excluded as observationally subsumed: the history diff that
  checks I7 and I8 is the same check that would check I9, so listing it
  adds no independent observable.
- I12 is excluded as derivative: it is the composition of I4 with I10 and
  I11, not an independent check; an adopter satisfying those three
  satisfies I12's conclusion.

**Period clause.** "First sustained operating period" is defined as the
interval ending with the adopting loop's Nth full ladder transit (one
artifact from draft through every agent-run gate into
awaiting-completion), N fixed in advance in the adopting loop's own
register, per the method's own pre-registration posture. Proposed default
N = 3, with the grounding stated at its true strength: the primary
instance's recorded window (two artifacts to awaiting-completion, three
same-day review loops; E10-C02/C09) grounds the order of magnitude, not
the constant. Fixing N in advance is what closes the memo's escape hatch
(a null re-attributed to insufficient operation); the constant itself is
a drafting decision A2/A0 may set.

The block states explicitly what it does not supply: the
counterfactual-free restatement of criterion (i) and the null semantics
are A2's restatement leg per the L107 routing.

## Reversals

- Considered adding the parenthetical origin clause only to I1 rather
  than to Definition 1's index declaration; reversed, because Definition 4
  places its temporal index on the invariants' scope declaration, and the
  memo instructed the mirror of that pattern. The Definition 1 clause
  carries the origin; I1 received only the two-word conforming edit.
- Considered leaving the trace map's Section 3.4 line untouched to keep
  the diff minimal; reversed on GR-9 grounds, since the new [E10-C12]
  marker in I12 would otherwise be unrecorded in the consumption map.
- Declined (not merely deferred) the MINOR-1 alternative route of
  extending Definition 1 with the register-wins rule: checked against
  E10-C12 and E10-C17, the rule is carried as prose governance and the
  extension would exceed the extraction.

## Checks

All four on the explicit path
`rehydrations/academic/conjecture_governance_method.md`, this session,
after the edits: check_versions PASS, check_register_refs PASS,
check_tier_vocab PASS, check_figures_fence PASS (exit 0 each; invocation
with the explicit file argument, non-vacuous per the argv-less guard).

## Handoff

- **WP-27, next action:** A2 seat runs its prose legs on the same file
  (MAJOR-1 reword checked verbatim against E10-C24; MAJOR-3 restatement
  consuming and deleting the `[A2: place in section 7]` block; MAJOR-4
  availability paragraph per the A0 ruling; MINOR-3, MINOR-4, MINOR-6),
  then bumps the draft version; A0 targeted re-check per the memo's
  five-leg successor criterion follows.
- **Open question (A2/A0):** the period clause's constant N = 3 is a
  proposed default; whoever places the final Section 7 wording should
  either adopt it or set another value, and the choice is theirs, not
  formalism's.
- **Blocked:** nothing. The A3 legs are complete; the proposed ledger
  entry is in the seat's final report to A0, not filed (ledger appends
  serialise through A0).
