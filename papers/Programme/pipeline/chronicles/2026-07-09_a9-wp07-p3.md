---
date: 2026-07-09
role: A9
wps: [WP-07]
extractions: [E1, E2]
register_head: C96
ledger_entries: []
---

# A9 · WP-07 P3 consistency station · linear_cap_paper.md at revision-draft-v2

## Verdict

**P3 BLOCKED, on one body finding.** The flip to awaiting-P4 is not recommended this pass. The finding is a single sentence, the section 3.5 preamble at line 207, which asserts blanket per-requirement necessity coverage ("section 5's necessity propositions show what fails when each is dropped") in direct contradiction of the revision-1 honesty discipline carried at lines 80 and 350, where the paper twice states that no necessity construction is offered for ER-1 or ER-3. Everything else audited is green: the four deterministic checks pass suite-wide, all statement cross-references resolve, the trace map is faithful to the extractions and to its cited ledger entries, and every citation key resolves against the bib. Four frontmatter staleness items are reported separately for A0 amendment; they are administrative, not body findings. No fix was applied at this seat (GR-6; P3 discipline).

## The finding (F1)

Line 207, section 3.5 preamble: "Each requirement is an obligation on an implementation, not a fact about the world; section 5's necessity propositions show what fails when each is dropped."

The antecedent of "each" is "each requirement", and section 3.5 lists seven (ER-1 to ER-7). The paper's necessity constructions are exactly three: Proposition 4.3 (budget form, ER-5, and it sits in section 4, not 5), Proposition 5.5 (erasure, ER-2), and Proposition 5.5b (provenance, ER-4). Lines 80 and 350 state explicitly that no necessity construction is offered for ER-1 or ER-3. The sentence is therefore a claim-strengthening residue of the pre-review "necessity both ways" framing (L065 era), surviving because the section 3.5 preamble was on neither revision card (A3's, L078, touched the section 4 preamble and the ER statements; A2's, L079, was scoped to abstract and sections 1-2/6-7). It is precisely the class of error the revision was cut to remove, and precisely the class this station exists to catch. A referee reading 3.5 and then 5.2 meets a contradiction.

What satisfies: a one-sentence micro-touch aligning the preamble to the three-of-five wording, for example "the necessity constructions of sections 4 and 5 show what fails when the budget form, erasure, or provenance is dropped alone; no necessity construction is offered for ER-1 or ER-3". Routing is A0's call.

## Evidence table (five-step pass)

| Step | Scope | Method | Result |
|---|---|---|---|
| 1. Four checks | tier_vocab, figures_fence, register_refs, versions over all 7 rehydration files | checks/ scripts, file list as argv | PASS x4, exit 0 x4. Note: invoked without argv the scripts exit 0 vacuously; the run of record passed the file list explicitly |
| 2. Internal consistency | numbering, naming, trio count, banned phrases | grep + line reads | Cross-refs to 5.5b/5.6/5.7/5.8 all resolve (statements at :345/:352/:355/:362; refs at :26/:29/:31/:32/:80/:90/:94/:199/:276/:350/:356/:379/:400/:404). "Open Problem 5.8" naming consistent file-wide; "Conjecture 5.8" zero matches. Trio counted as three at :70/:80/:90/:350/:400 and Remark 5.7 fence one. "even without erasure" zero body matches (appendix :456 quotes it only as removed history). No blanket "auditable" (:110 differentiates by requirement; :386 owns the ER-5 gap). **FINDING F1 at :207** (above) |
| 3. Trace map vs body | every claim marker vs E1/E2; revision annotations | extraction reads, row-by-row | All markers resolve (E1 c01-c06/c08/c09/c10/c12; E2 c01/c02/c04/c11/c12). Revision annotations match the body: L069(b) sup-semantics caveat present in Remark 3.4; L073 E1-c12 in extraction_basis; Prop 4.2 hypotheses (i)-(iii) + inline counterexample; Prop 4.3 surfaced as unconditional separation; Cor 5.4 threshold reconciliation + certification clause; Prop 5.5b adopted; ER-1/ER-6 rewordings; L079 csiszar residual micro-touch confirmed landed at :128 and :209 |
| 4. Frontmatter vs manifest | status, extraction_basis, bib pointer, gate_target, handoff | line comparison, manifest read-only | Four mismatches reported for A0 (below); bib pointer resolves; manifest WP-07 status/gate (revision-draft-v2, P1) coherent with the file |
| 5. LaTeX sanity | mapping note scaffold + bib key presence | ls + key-presence grep, no compile | Mapping note names templates/submission/preprint/ sections 03_model_preliminaries, 04_theorems, 06_threats_to_validity; all exist. 12/12 citation keys used in the draft resolve in pv_v6.bib |

Role sweep-list extras, this run: register head C96 agrees across manifest (:4) and canon register (:5); retired citations (Research Paper v4.0/v4.2) absent; static-ceiling family absent (the :377 match is the negative self-commitment, GR-7 compliant); em-dashes zero; fenced figures zero; no URLs in the draft (TIER-A allowance unused); no emoji in formal statements.

## Frontmatter mismatches (A0 to amend; not body findings)

1. `extraction_basis` omits E2-c04 while the trace map's Remark 3.4 row cites it (sup-semantics caveat, L069(b)). Same class as L073, which fixed the E1-c12 omission; the E2-c04 parallel was not caught then.
2. `gate_target: P1 (A5-crypto-pc review)` is stale: P1 passed per L080; the current station is P3, target awaiting-P4.
3. `handoff: A5-crypto-pc` is stale: review 2 was superseded by the A0 targeted re-check under A5's own ruling (L080); the post-P3 handoff is the P4 owner.
4. Status-line tail "Ready for A5-crypto-pc review 2" is stale for the same reason.

Candidate fifth, A0's judgement: manifest WP-07 carries `extraction: E1` while the paper draws E2 claims as well; other manifest rows use list form, so `[E1, E2]` is expressible.

## Extraction-side hygiene (for A0 to route to A1; non-blocking here)

E2-C02, E2-C04 and E2-C12 are consumed by WP-07 (frontmatter and/or trace map) but their FEEDS lines do not name WP-07. Under the L031/L065(d) discipline that FEEDS tracks flows, these three lines want reconciliation at the next E2 maintenance touch.

## Observation (non-blocking)

The trace map's Remark 5.7 row calls the revision-1 guarantee-semantics fence the "third fence", following L078's wording ("with a third fence" in the cardinal sense), while the body orders it second and cross-references it as "second fence" (:276, :379). The body is internally consistent and the apparatus matches its cited ledger entry; recorded so no future reader mistakes it for a numbering drift.

## Reversals

One near-reversal recorded per GR-8: the first check invocation (no argv) returned exit 0 with no output, which would have been vacuous P3 evidence had it been accepted; the scripts iterate over argv and pass trivially on an empty list. Caught by the absence of the expected PASS lines, re-run with the explicit file list. A9 flags the argv-less silent pass as a check-harness sharp edge worth a guard (no script change made this session; role card failure-mode rule).

## Handoff

- **WP-07, single next action:** A0 routes the F1 micro-touch (one sentence, :207) and amends the four frontmatter lines; A9 then re-runs targeted (the :207 read, the four checks, frontmatter re-compare). Clean re-run = P3 GREEN, flip to awaiting-P4, runtime touches WP-07 no further.
- **Open question:** whether A0 also takes the manifest `extraction:` list-form amendment and the E2 FEEDS reconciliation this cycle or lets them ride the weekly sweep.
- **Blocked:** the P3 flip, on F1 alone.

---

## Addendum · targeted P3 re-run per L081(d) (same day, same seat)

### Verdict

**P3 GREEN. F1 resolved; zero frontmatter/manifest mismatches remain. The flip to awaiting-P4 is recommended; execution is A0's.**

### Re-run evidence

1. **:207 in place.** The preamble now reads: "The necessity constructions of sections 4 and 5 (Propositions 4.3, 5.5 and 5.5b) show what fails when the conditional budget form (ER-5), erasure (ER-2) or provenance (ER-4) is dropped alone; no necessity construction is offered for ER-1 or ER-3, whose sufficiency is claimed only in conjunction with the rest." This is the three-of-five form; it attributes sections 4 and 5 correctly (Prop 4.3 lives in section 4); it agrees with :80 and :350 in substance, including the conjunction-sufficiency clause mirroring :350. No conflict remains.
2. **Checks.** Four checks re-run with the explicit seven-file list (non-vacuous: 7 PASS lines per check confirmed): tier_vocab, figures_fence, register_refs, versions all exit 0; combined 0.
3. **Frontmatter vs manifest.** Zero mismatches. extraction_basis now lists E2 c04; gate_target reads "P3 -> awaiting-P4 (P4 is the First Person's; the runtime stops here)" and handoff "A9 P3 re-run, then the P4 owner", both coherent with the manifest note and the task card (wording is A0's, judged on coherence per instruction); status tail records the L080 supersession of review 2 and the L081 station state; manifest extraction is now [E1, E2]. Manifest gate remains P1, correct until A0 executes the flip.
4. **Ledger.** L081 confirmed appended by A0 (:488), matching the entry proposed in this chronicle's first pass; the E2 FEEDS hygiene rides the next A1 touch per L081(c). The A3 chronicle addendum carries a GR-8 vacuity disclosure for its earlier argv-less check runs, per A0's report.

### Handoff (supersedes the block above)

- **WP-07, single next action:** A0 marks P3 passed and flips WP-07 to awaiting-P4. The runtime then touches WP-07 no further; P4 is the First Person's.
- **Open items riding elsewhere:** E2 FEEDS reconciliation (next A1 maintenance touch, L081(c)); the check-harness argv-less silent-pass guard (weekly sweep candidate, no script changed this session).
- **Blocked:** nothing.
