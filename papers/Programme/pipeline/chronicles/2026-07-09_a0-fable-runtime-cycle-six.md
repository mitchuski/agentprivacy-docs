---
date: 2026-07-09
role: A0 (standing orchestrator; cycle 6 cut and closed same-day)
wps: [WP-03 (pending -> release-draft-v3/P1, chain COMPLETE), WP-04 (untouched at awaiting-P4; prior_art apparatus refreshed), WP-07 (untouched at awaiting-P4; frontmatter levelled)]
extractions: [E10 (BUILT, drafted, 18 claims — the WP-27 seed)]
register_head: C96
ledger_entries: [L095, L096, L097, L098, L099, L100, L101]
sessions: 6 seats (A10, A1, A9, A2-draft, A7, A5-assessor, A2-revision = 7 seat-runs across 6 sessions) + the standing A0
companions: [2026-07-09_a0-fable-runtime-cycle-five.md, programme/NOTE_FABLE_RUNTIME_2026-07-07.md]
---

# A0 session · cycle 6 closes: the tools are proven sharp, the method record exists, and the grant edition runs its whole chain in a day

## Verdict

Cycle 6 was cut as a maintenance cycle and closed as the runtime's third
full-chain day. The three hardening items queued by cycles 4 and 5 were
fixed at the A0 seat and verified twice (once by synthetic probe at A0,
once independently at the A9 station); the prior-art apparatus was
reconciled with the citation record; E10 method-record was built, giving
WP-27 its extraction basis; the weekly sweep came back green 32/32 with
E10 entering clean; and WP-03, unblocked since 2026-07-03 and untouched
since, ran draft → P0 → voice → adversarial assessor review → same-day
revision → targeted re-check → P1 in a single cycle, completing its
chain. Everything that remains on WP-03 is a first-person act: budgets,
the applicant block, and the read.

## The stations

- **A0 hardening (pre-seat):** the argv-less guard (L082(c)) now makes a
  pathless check run exit 2 with a loud refusal at all four entrypoints;
  the STATIC pattern split (L091(d)) makes the R<1 arm word-bounded,
  case-sensitive and digit-run-refusing while the ceiling arm keeps its
  case-insensitivity. Verified by synthetic probes in both directions
  before any seat ran; the prior_art :62 false positive died without
  weakening the GR-7 catch.
- **A10 prior-art refresh (L095):** thirteen rows reconciled with the
  A4-verified metadata, metadata only, zero verdict changes; the
  enrichment widening ratified at serialisation (the card's full-diff
  method line governs; every enrichment traces to a verified bib entry).
- **A1 E10 build (L096):** 18 claims in the E1 format at head C96; the
  earned-twice method rules carry both earning events (pipeline and
  circuit-workshop); Cx-a/b/c honestly NOT extracted pending the
  first-person adoption ruling; coverage gaps stated in the file's own
  header; seven sources deferred to a cycle-7 widening card. A0 ruling:
  the two process-record STATUS values admitted for E10 only.
- **A9 weekly sweep (L098):** GREEN, 32/32 cells non-vacuous; all three
  hardening items independently VERIFIED (L082(c) and L091(d) CLOSED);
  register head triple-agrees C96; both awaiting-P4 papers' trace maps
  fully resolve; E10's first sweep entry clean. Its two A0-owned
  findings executed at serialisation: WP-07's frontmatter levelled to
  awaiting-P4 (the L093 class) and the manifest's duplicated deadline
  clauses deduplicated.
- **WP-03, the full chain (L097-L101):** A2 draft-v1 (eight work
  packages converting every unmeasured E1/E2 quantity into deliverable +
  method + evaluation criterion; budgets as first-person placeholders;
  T3 carried as structure only) → A0 P0 spot-trace 3/3 faithful → A7
  voice pass to draft-v2 (one honest weakening; its E1-C09 "compounding"
  flag adjudicated against the extraction and micro-touched at A0
  BEFORE the review — the fix-before-review lesson holding) →
  A5-assessor review 1 = MAJOR REVISION, 0 BLOCKING / 4 MAJOR / 5 MINOR
  (proven core traced clean at every load-bearing site; the MAJORs
  cluster exactly where an assessor's rubric bites: two evaluation
  criteria that could not fail or could not fire, asserted independence
  with undeclared interfaces, a data plan without a feasibility
  statement) → four A0 programme-shape rulings → A2 revision-1 to
  draft-v3, all nine findings to their what-satisfies lines, three
  narrowing extensions recorded → A0 targeted re-check CLEAN over all
  nine anchors with checks re-run at this seat = P1. The chain
  (A9-first, A2, A7, A5-assessor) is complete; the runtime touches
  WP-03 no further.

## Harness lessons banked this cycle

- A hardening fix is not done when it is applied; it is done when a
  different seat fails to break it. The A0-then-A9 double verification
  cost one extra station and bought the closure of two standing items.
- The fix-before-review lesson generalises across tiers: the A7 flag
  adjudicated at A0 before the assessor read was a finding the review
  memo never had to spend (the review still found nine — reviewer
  attention goes where the pre-review repairs are not).
- The reviewer's successor criterion again replaced a second full
  review; three papers, three clean targeted re-checks (L080, L092,
  L101).
- Programme-shape defects (a criterion that cannot fail, an undeclared
  dependency) are A0-ruling material, not prose material: routing them
  to A2 without rulings would have produced nine guesses.

## State at close

WP-07 awaiting-P4 (PoPETs; read by ~08-24 for Issue 2). WP-04
awaiting-P4 (FC workshops 09-01). WP-01 post-ready-v5 (posting = first
person). WP-02 at P1 awaiting the author block. **WP-03 release-draft-v3
at P1, chain complete** (budgets + applicant block + read = first
person). E10 drafted (widening pass = cycle 7). Hardening list: EMPTY —
L082(c), L091(d), L093(b) all closed. Ledger head L101, register head
C96.

## Handoff

- **Cycle 7 (runtime, when cut):** E10 widening pass (seven deferred +
  two partial sources) · WP-02 release-strip on author-block receipt ·
  WP-27 A2/A3 seats once E10 widens · WP-05/WP-26 adaptation from the
  WP-03 base on first-person direction · next A9 weekly adds
  grant_edition.md to the sweep list.
- **First person (8 items):** WP-07 P4 read + PoPETs aim · WP-04 P4
  read (FC 09-01) · WP-01 read + post · WP-02 author block
  (2026-08-01) · register brief (a)-(e) · Cx-a/b/c adoption ·
  **WP-03 budgets + applicant block + completion read** · harness
  canon-integration decision points 1-6 (L094).
