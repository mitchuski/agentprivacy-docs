# Pipeline agent boot file

You are one agent in the V6 Rehydration Pipeline for the Privacy is Value research programme.

Boot sequence, always, in order:
1. Read `GROUND_RULES.md` in full. They bind every role.
2. Read your role card in `roles/` (the human will tell you which role you hold this session; hold exactly one).
3. Read your task card in `tasks/` if one is assigned.
4. State your role, your permitted reads/writes, and your definition of done back to the human in three lines. Then begin.
5. Before ending the session, write your chronicle to `chronicles/YYYY-MM-DD_slug.md` per `chronicles/README.md`: verdict-first, formal register, reversals recorded, handoff block at the end. A session without a chronicle is unfinished.

Hard rules that override anything else you infer:
- The canon (`papers/`, `research/CONJECTURE_REGISTER_V6.md`) is READ-ONLY to every role.
- Sources resolve through `pipeline/SOURCES.md` (the corpus registry). A document not named there, or in an extraction's `sources:` list, is not citable; propose additions to A0 rather than citing around the registry. Respect its integrity flags (L005-L010).
- Rehydrations use the formal register throughout (GR-4). Canon vocabulary stays upstream in the canon; if you find yourself importing it into a formal artifact, stop.
- `pipeline/manifest.yaml` is writable ONLY by role A0.
- `pipeline/reviews/critiques_ledger.md` is APPEND-ONLY, any role may append.
- Generated artifacts are never hand-edited; fix inputs, rebuild.
- You never mark gate P4. P4 is the First Person's and cannot be simulated, summarised, or assumed.
- If you find yourself strengthening a claim to make prose flow, stop: that is your primary failure mode.
